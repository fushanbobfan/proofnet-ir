# v0.10 unification completeness design

Status: active development
Baseline release: v0.9.0

## Purpose

v0.10 targets the exact gap intentionally left by v0.9: prove that the pure
event-driven token-unification path succeeds on every correct unit-free,
cut-free MLL certificate, without appealing to recursive inverse-rule
reconstruction. Complexity claims remain a later layer. A universal
completeness theorem must not be inferred from the existing 7,200-case
counterexample search.

## Formal layers

### 1. Independent rule semantics

Define a proof-irrelevant marking/partition state and an inductive one-step
relation with exactly three rules:

- axiom/start assigns one fresh token to both unmarked axiom conclusions;
- par/forward marks a conclusion when its two premises have the same current
  representative;
- tensor/unify marks a conclusion and merges two distinct premise classes.

The relation must state freshness, range, and occurrence bounds without
referring to the executable scan or worklist functions. Prove that every
executable firing refines one semantic step. Separately retain the existing
derivation-component invariant so every fired token class denotes a genuine
partial cut-free derivation.

The abstract relation uses a fixed `Nat` carrier and an allocated initial
segment. Allocating token `tokenCount` therefore preserves the global thread
equivalence and separately proves that this carrier element was isolated from
all previously allocated tokens. Defining the extension only on the enlarged
initial segment is invalid here because it would violate reflexivity outside
that segment.

### 2. Scheduler coverage

State an invariant connecting every unmarked connective to one of:

- idle because at least one premise is unmarked;
- queued because a premise was newly marked;
- waiting par because both premises are marked with distinct classes;
- permanent tensor deadlock because both premises have the same class;
- already fired.

Prove initial connective enqueues establish the invariant, marking a
conclusion enqueues all newly affected consumers, and every tensor merge
requeues all pars whose representatives may have changed. This theorem is
about absence of missed enabled work, not proof-net correctness.

### 3. Fuel sufficiency

Prove bounds for successful enqueue sources:

- at most one initial enqueue per connective;
- at most one distinct dependency enqueue per successful firing, because
  structural linear ownership makes every premise-consumer bucket constant;
- at most one submitted-link carrier of waiting-par requeues per successful
  tensor firing.

Use queue deduplication and single-firing of connective conclusions to show
that the existing `n(n+4)+1` fuel reaches an empty queue rather than merely
stopping the recursive evaluator.

### 4. Correct-state progress

Connect the independent marking semantics to the already proved switching
criterion. For a structurally well-formed correct certificate, prove that a
quiescent nonfinal state cannot contain a tensor deadlock and cannot contain
only waiting/idle links. Therefore scheduler quiescence implies total marking
and one token class.

Combine this with the existing independent derivation verifier to prove:

```text
Certificate.unificationWorklistFastCheck = Certificate.check
```

Only after this theorem is checked may the recursive reconstruction fallback
be removed from the logical decision.

### 5. Sequential linear implementation

Pure completeness does not imply Guerrini's linear-time theorem. Replace eager
all-axiom starts and flat waiting requeues with the Figures 7--8 stack:

- build later-state start selection around the now-kernel-checked
  bounded/tagged `NEXTAXIOM`. Per-call trace/tag invariants, exact oriented
  source-left routes, and touched-set disjointness for successive calls that
  strictly thread `first.tags` are already proved. Initial/local totality is
  proved under rank-scoped freshness with fuel greater than the start
  complexity. It does not establish freshness for a later Figure-7 `new` call
  or totality of the carrier-size wrapper. Since any success tags
  complexity-zero axiom endpoints, this global low-rank predicate cannot be
  threaded to a second call; introduce a route-local scheduler invariant;
- retain the now-kernel-checked first delayed state slice:
  `RawTokenAge` is discovery order rather than a representative,
  `SigmaAgePartition` encodes strictly increasing raw-age boundaries below the
  horizon, and fixed-capacity waiting storage distinguishes out-of-bounds,
  undefined `⊥`, and initialized empty `∅`. Strict empty `init` leaves `W(0)`
  undefined. The printed Figure-7 `new` writes the fresh top even though the
  prose defines `W` on inactive boundaries and `unify` reads the old
  predecessor. Keep `newEnqueue?` only as a literal source-audit helper.
  Production `operationalNewEnqueue?` initializes the old active boundary,
  leaves the fresh top undefined, and preserves the kernel-checked
  `OperationalWaitingDomain` stating that initialized allocated cells are
  exactly `sigma.dropLast`. This is the project's operational interpretation,
  not an author-confirmed erratum;
- the production bridge implements initial and fully tag-threaded typed later
  reservations, proves the narrow `RealizesSigma` relation between delayed and
  production state, threads the complete output tags, and preserves
  `ReservationInvariant`, including `OperationalWaitingDomain`. The local
  Figure-7 pipeline also synchronizes pop-before-mark, raw-age marking,
  tensor-mate selection, post-mark `NEXTAXIOM`, and operational `new`. These
  steps now prove that every successful deterministic `NewStep`, and every
  executable `new?` result, preserves the complete current occurrence-exact
  state-only `SchedulerInvariant`, including its exact component forest and
  queue/causal/waiting/pending/counter fields. This preservation theorem alone
  does not characterize reachability, prove that `new?` succeeds on every
  intended later state, or prove later-state search totality. The separate
  `InitNewHistory` now records exact
  empty/init/new executions and proves tag provenance, global submitted-slot
  non-reuse, and reservation-count alignment. Every successful local `wait`
  now also preserves the complete current `SchedulerInvariant`: exact submitted
  par position, global queue uniqueness/raw-unmarkedness, strict waiting span,
  unchanged occurrence forest, and unchanged firing counter. Exact local
  successful `forward` now also preserves the complete invariant while
  constructing the exact submitted par, replacing its live frontier, updating
  active ready, transporting the queue/waiting/pending fields, and incrementing
  the exact fired counter. Its paper guard is the non-strict
  `selectedRawAge ≤ mateRawAge`; the additional active-ready `Nodup` test is
  only a fail-closed shape check. A non-equality boundary theorem and typed
  `init → nop → forward → concl` regression are locked. The independent
  Boolean-free `ForwardRule` now retains the exact submitted par occurrence,
  occurrence picks, paper guard, and output equations without referencing a
  Figure-7 executable or mutation wrapper. Executable soundness and
  structurally valid completeness/iff/output uniqueness are kernel checked
  with `ForwardExecutableReadyNodup` supplied separately; the complete
  `SchedulerInvariant` derives that representation condition and yields the
  higher-level completeness/iff. The bounded empty-cell `UnifyEmpty` executable
  and independent direct relation are now also kernel checked: soundness uses
  `ReservationInvariant`, while structural validity plus that invariant and a
  separate ready-list `Nodup` premise gives completeness/iff/output uniqueness.
  Successful typed and executable bounded steps preserve the complete current
  occurrence-exact `SchedulerInvariant`, including `RealizesSigma`, exact
  survivor/retired component-forest ownership, queue/waiting/pending facts, and
  the fired counter across the active-boundary pop and `parent[i] := j`.
  Exact local
  `concl`/`nop`/`wait`/`forward`/`UnifyEmpty`/`UnifyOne` transitions still sit
  outside that
  history; these are local successful-step results, not applicability,
  totality, dispatcher, history, reachability, progress, pure-worklist
  completeness, fallback removal, `NEXTAXIOM` sequencing, or linearity;
- strict-singleton `UnifyOne` is now formalized for exactly `W(j) = [c]`:
  exact producer/source-slot lookup, atomic prepare → tensor union → one
  waiting-par activation → scheduler drain, independent Boolean-free direct
  relations, typed/executable correspondence and unique output, exact `+2`
  counter accounting, and preservation of both `ReservationInvariant` and the
  complete occurrence-exact `SchedulerInvariant`. Empty and payload length at
  least two fail closed. The explicit par construction is the project's
  derivation/provenance representation refinement; the paper itself specifies
  moving `W(j)` into ready;
- the local arbitrary ready/waiting payload production-core fold is now
  formalized with deterministic stored head-to-tail execution, independent
  direct and typed relations, exact executable correspondence/output
  uniqueness, documented core-field preservation, and
  `firedConnectives += payload.length`. This is not a paper ordering claim and
  does not by itself establish scheduler applicability, occurrence-forest
  preservation, or an atomic general `Unify`;
- `SequentialFigure7UnifyPayload.lean` now integrates that fold into one local
  atomic tensor/fold/drain transition. Its high-level-executable-independent
  direct rule, typed witness,
  and executable have exact correspondence/output uniqueness under the stated
  premises; every success preserves `ReservationInvariant`, and the exact
  state equation adds one tensor plus `payload.length` pars. Old
  `UnifyEmpty`/`UnifyOne` success maps one way to the same new output without
  executor equality or reverse equivalence. The follow-on
  `SequentialFigure7UnifyPayloadInvariant.lean` proves complete
  `ComponentForestProvenance`/`SchedulerInvariant` preservation for successful
  typed/executable steps from a full input invariant. Its non-circular
  fixed-final-stack gap derives pre-activation freshness and exact
  producer/boundary facts, establishes ownership head by head, and closes at
  the empty suffix; no history/reachability premise or intermediate-state
  invariant is claimed. The follow-on
  `SequentialFigure7UnifyPayloadEnabled.lean` defines pure input-only
  `UnifyPayloadEnabled` and proves that it plus the full invariant yields
  executable success and an invariant-preserving output. The invariant alone
  does not imply the predicate. Stored execution order is not a commutativity
  or paper temporal-order theorem;
- `SequentialFigure7StableEnabled.lean` defines pure input-only
  `ConclEnabled`, `NopEnabled`, `WaitEnabled`, and `ForwardEnabled`. The full
  invariant derives the hidden prepare state, wait destination/payload, and
  forward token/component/pick/representation guards, giving both executable
  success and a full-invariant output. Its submitted-par trichotomy applies
  only after an exact ready head and submitted par are supplied;
- `SequentialFigure7Dispatcher.lean` now integrates the canonical successful
  `concl`/`nop`/`new`/`wait`/`forward`/`unifyPayload` executors under a fixed
  precedence. Priority-aware typed witnesses, exact executable correspondence,
  output uniqueness, complete invariant transport, and a proof-carrying
  certified history are kernel checked. Legacy empty/singleton unifiers are not
  duplicate history constructors. This history records only supplied-invariant
  successful executions and therefore proves no applicability or progress;
- **Historical priority checkpoint, superseded by the input-only migration
  below:** `SequentialFigure7PriorityEnabled.lean` supplied exact applicability
  correspondence for that fixed priority order. Typed successes for
  `concl`/`nop`/`wait`/`forward`/`unifyPayload` reconstructed the existing pure
  enabled inputs, and each executor had existential-success iff under the full
  invariant. At this checkpoint `NewExecutableEnabled` was the deliberately
  operational existential-`new?` field stored by `PriorityEnabled`.
  `PriorityEnabled` was equivalent to the matching `DispatchStep`,
  characterized the exact selected kind and the no-branch case, and had a
  unique kind. The completed reachable `[[]]` counterexample kept
  intended-state exhaustiveness and progress explicitly open;
- **Historical priority-state note, superseded below; the necessary projection
  itself remains current:** `SequentialFigure7NewInputNecessary.lean`
  extracted the input-only necessary content of a successful `new`: a
  ready/tensor/unmarked-mate guard
  plus a bounded exact source-left route with input tag freshness, whole-trace
  production readiness, and ready axiom endpoints. Success implies
  `NewInputNecessary`, never conversely. The witness omits recursive per-step
  tag-update equations, terminal-partner exclusion from the intermediate
  trace, and the later operational enqueue guard; all-true and
  partner-pretagged regressions keep the shallow guard but
  make `new?` fail. At this checkpoint dispatcher priority still stored the
  operational proposition; the later `NewEnabled` migration below supersedes
  that priority-state claim;
- `SequentialFreshSourceLeftRun.lean` now gives the production source-left
  search an exact proof-relevant input semantics. Its four constructors mirror
  the executable terminal-left, terminal-right, tensor-left, and par-left
  branches, retaining exact source buckets, submitted slots, recursive tag
  updates, fixed raw-mark readiness, and terminal orientation. Lean proves
  exact named execution iff a run exists, plus trace freshness/`Nodup`, partner
  exclusion, and a structural/carrier-guarded terminal reservation bridge;
- `SequentialFigure7NewEnabledCore.lean` combines `NewGuard`, a
  certificate-fuel-bound `FreshSourceLeftRun`, and the exact selected-raw-age
  enqueue predicate. This `NewEnabled` is input-only: it contains no executor
  result/equation, output, history, or reachability field. Under
  `SchedulerInvariant`, it is equivalent to existential `new?` success and
  yields an invariant-preserving output. Raw-marked-intermediate,
  terminal-partner-pretagged, and queued-partner regressions delimit the exact
  criterion. `SequentialFigure7NewInputCore.lean` supplies the dedicated
  lower layer, allowing `PriorityEnabled` to store `NewEnabled` in its positive
  `new` branch and `¬ NewEnabled` in every later branch. The operational
  `NewExecutableEnabled` definition, exact iff, and compatibility constructor
  remain public, and the dispatcher executable/order are unchanged. The
  historical `SequentialFigure7NewEnabled.lean` facade preserves its former
  direct-import priority surface; compile-only default targets guard both it
  and the narrow priority import;
- `SequentialFigure7ProgressInvariant.lean` separately preserves
  `FutureWaitingUndefined` from exact initialization through every successful
  dispatcher history. `SequentialFigure7NewRegion.lean` proves that structural
  well-formedness reconstructs the exact run from `FreshSourceLeftRoute` and
  derives terminal-partner exclusion. Its input-only source-region record adds
  only two post-pop endpoint queue-absence facts and strict fresh waiting
  capacity; together with `SchedulerInvariant` and the future-cell invariant,
  those imply `OperationalNewReadyAt` and `NewEnabled`. The later
  `SequentialFigure7FreshCapacity.lean` derives strict capacity from structural
  well-formedness, canonical reservation history, and an exact current-tag run.
  `SequentialFigure7QueueHistory.lean` derives both endpoint queue-absence
  facts by tracking exact axiom endpoints through every dispatcher branch; it
  intentionally does not claim that arbitrary queued connective conclusions
  are tagged. Canonical history plus the complete invariant therefore proves
  `NewEnabled ↔ NewInputNecessary`, and certified dispatcher reachability
  packages the same equivalence. This remains conditional on the exact route
  stored by `NewInputNecessary`, not a theorem that shallow `NewGuard` supplies
  that route or makes progress;
- `ProofNetIRNewProgressAudit.lean` now audits the complete ready-head boundary
  over a deterministic finite generated corpus. Every post-initialization
  canonical replay state is classified by concrete marking completeness and
  exact typed `ReadyHeadInput` availability before the real dispatcher is
  called. Incomplete states without a head and incomplete dispatch-none stops
  retain complete replayable witnesses and fail closed. Default CI uses seed 0,
  depths 0 through 4, all starts, and six labelled reorder variants;
  `--extended` adds depth 5, while `--cross-representative-search` widens the
  labelled variants. The default, extended, and cross receipts classified all
  observed incomplete states as ready-head/successful-dispatch states and all
  dispatch-none stops as fully marked, with zero missing-head,
  incomplete-dispatch-none, cycle, or truncation findings. Deep candidates use
  `unificationCheck` with `unificationCheck_eq_check` kernel transport, and
  depths 0 through 2 retain 18 direct all-switchings differential sentinels.
  Guarded-New and marked-tensor-predecessor checks remain nested subaudits. This
  is bounded falsification evidence only, not a semantic completion, progress,
  later-state totality, or completeness theorem;
- `SequentialFigure7TagHistory.lean` derives branch-aligned tag evidence from
  that exact history: stable branches preserve tags, `new` records its exact
  search touch and submitted slot, and the complete trace proves exact tag
  provenance, touch separation, history independence, slot `Nodup`, and exact
  recorded reservation-slot length equal to final `nextAge`, without adding
  reachability or progress;
- `SequentialFigure7RawMarkHistory.lean` reuses that branch-indexed carrier to
  expose every successful branch's common prepared prefix and exact selected
  occurrence/raw-age event. Lean proves one-step old-or-current-event mark
  transport, final raw mark iff a canonical event, and history/reachability
  event recovery. `RawMarked` is separate from `Touched`, since stable rules
  mark connective conclusions without `NEXTAXIOM`. This provenance layer does
  not provide queue origin, vertex-level commitment paths or target avoidance,
  derive a created-candidate seam, or prove progress. The independent
  retained-`sigma` commitment spine below does not change those local limits;
- `SequentialFreshSourceBlocker.lean` now classifies source-left search
  structurally. Given `StructurallyWellFormed` and an in-bounds start, it
  returns either a formula-budget `FreshSourceLeftRun` or a nonempty
  `FreshSourceBlocker`. The structural region contains the recursively visited
  stored-left vertices and the terminal submitted axiom's partner. Its only
  dynamic obstruction is an input tag lookup other than `some false` or a raw
  mark lookup other than `some none`; source shape, source singletonhood, and
  the formula fuel bound are discharged structurally. Queue separation and
  fresh capacity remain post-run obligations. This theorem assumes no
  scheduler history, declarative correctness, or reachability and proves no
  blocker exclusion, enabledness, progress, totality, or completeness. A
  separate corollary turns uniform region availability into the positive run,
  isolating exactly what the later canonical-history theorem must establish;
- `SequentialComponentSourceLeftGeometry.lean` now proves the complementary
  occurrence-carrier closure. If a source occurrence is owned by an exact
  `OccurrenceDerivation`, every recursively visited stored-left occurrence and
  the terminal axiom partner is owned by that same derivation. This structural
  theorem identifies no scheduler component and proves no chronological
  separation, reachability, enabledness, or progress;
- `SequentialFigure7BlockerHistory.lean` now classifies each such dynamic
  blocker under `CanonicalTagHistory`, the full `SchedulerInvariant`, and the
  selected `NewGuard`. A tag failure is a recorded prior touch. A raw-mark
  failure is either the selected ready-head update or an old concrete raw mark
  with occurrence-exact ownership in a live component. The three forms may
  overlap. An explicit premise excluding all three on every source-region
  vertex yields the exact run, then `NewInputNecessary`, then `NewEnabled`;
- exact formula-complexity descent and a final-step decomposition now exclude
  a return to the selected ready head for every recursively `visited`
  occurrence under structural well-formedness alone. The visited blocker
  classification consequently retains only prior-touch or exact old-owner
  alternatives;
- `SequentialFigure7TerminalPartnerGeometry.lean` now supplies that distinct
  terminal exclusion under reference-switching acyclicity. Exact source-left
  and terminal-axiom occurrences form a simple all-left reference path avoiding
  the selected tensor conclusion; equality of terminal partner and ready head
  would close an occurrence-aware cycle with the tensor's two fixed edges.
  `DeclarativelyCorrect` packages the required acyclicity, and the complete
  blocker classification consequently has only prior-touch or exact old-owner
  alternatives;
- `SequentialFigure7TouchOrigin.lean` now turns the prior-touch alternative
  into one exact historical init/new event with its submitted link slot,
  oriented route, and complete old source-left region;
- `SequentialFigure7ReservationLedger.lean` now folds only authentic initial
  and `new` events into an oldest-first ledger. Its raw ages are exactly the
  full allocated range, every allocated age has an exact event lookup, its
  chronological slots reverse the legacy newest-first slot history, and every
  prior touch reaches an event that itself touched the vertex. A `new` event
  uses the fresh pre-state horizon and strictly follows its selected old active
  age. This is not a historical-reservation realization theorem, current-owner
  bridge, old/current region-disjointness result, or progress argument;
- `SequentialFigure7CommitmentSpine.lean` kernel-proves that every adjacent
  pair in the final retained `sigma` is backed by the exact historical `new`
  reservation event stored at the child raw age's chronological ledger slot.
  Stable rules preserve the spine, `new` appends its final edge, and
  `unifyPayload` removes only the popped active boundary. This is allocation
  ancestry only: it supplies no vertex-level reference path, target avoidance,
  queue origin, raw created-candidate seam, enabledness, progress,
  completeness, fallback removal, or complexity theorem;
- `SequentialFigure7TouchCompleteness.lean` now proves that a supplied exact
  source-left run covers its whole structural source-left region under
  structural well-formedness, and reconstructs that run from every authentic
  initialization or `new` reservation event. Consequently event touch is
  equivalent to event-region membership. The reconstruction depends on the
  successful equation retained by `ReservationEvent`, so it is not a theorem
  about a bare erased `ReservationSearchEvent`; it adds no current
  representative, ownership, created-region, enabledness, or progress claim;
- `SequentialFigure7ReservationRealization.lean` now closes the narrower
  realization bridge for exactly what a reservation submits. Under explicit
  certificate structural well-formedness, every ledger event has an
  event-indexed `OccurrenceDerivation` for the final live
  representative component containing that exact submitted axiom-link slot;
  the derivation's owned occurrence list agrees with the final component
  forest, and both exact axiom endpoints occur in it. This does not say that
  every historical search-trace vertex is owned, preserve separate event
  components after tensor unions, exclude a current route intersection, or
  prove progress;
- `SequentialFigure7RawMarkReservationAnchor.lean` now anchors every concrete
  raw mark to the authentic reservation event at the same immutable raw age.
  The mark and both submitted-axiom endpoints share one final representative
  component and exact owned carrier, and contained reference-switching paths
  connect the mark to both endpoints. This local theorem needs neither
  declarative correctness nor switching acyclicity, but it does not compose
  across commitment-spine components, prove target avoidance or queue origin,
  discharge a raw seam, or establish progress;
- `SequentialFigure7CommitmentEdgeReferencePath.lean` now reconstructs the
  exact historical `new` step behind every adjacent parent-child pair retained
  in final `sigma`. It composes the parent owned anchor, the selected-head
  tensor/NEXTAXIOM segment, and the child owned anchor into a canonical simple
  path between the two events' stored left endpoints, retaining exact final
  accounting on both sides. This one-edge theorem needs neither declarative
  correctness nor switching acyclicity, but it does not prove arbitrary
  future-target avoidance, package whole-spine composition, recover queue
  origin, discharge a raw seam, or establish enabledness, progress,
  completeness, fallback removal, or complexity;
- `SequentialFigure7CommitmentEdgeTargetAvoidance.lean` conditionally refines
  that single edge. Given a future candidate and an explicit law that the exact
  child ledger event does not touch its tensor conclusion, Lean rebuilds a
  canonical endpoint path omitting that conclusion. The module does not derive
  the child-event law or its global availability, compose arbitrary multi-edge
  paths, recover queue origin, discharge a raw seam, or prove progress;
- `SequentialFigure7CommitmentIntervalTargetAvoidance.lean` composes explicit
  adjacent avoiding witnesses across any supplied positive-length retained
  `sigma` interval. Exact ledger lookups identify shared middle events and
  verified loop erasure joins the paths. It does not derive or globalize the
  edge callback or child-event laws, cover a zero-edge interval, recover queue
  origin, discharge a raw seam, preserve segment or parallel-edge identity, or
  prove progress or complexity;
- `SequentialFigure7CrossRepresentativeInvariant.lean` now fixes the
  non-circular invariant that will handle strictly older historical regions.
  `FutureWorkAt` retains exact ready/sigma positions or exact initialized
  waiting cells; the full state invariant derives live `sigma` membership,
  allocation bounds, and the boundary's current representative-root fact.
  `FutureNewCandidateAt` retains an exact tensor consumer and input-unmarked
  mate. `OlderSourceRegionSeparated` compares only strict order between the
  event and candidate's *current union-find representatives*. It intentionally
  adds no raw allocation-age inequality and no symmetric `≠` requirement.
  Empty history and every exact initialization satisfy it, and a strictly
  older event touch is excluded from a candidate region;
- `SequentialFigure7OlderEventTouchSeparation.lean` now presents that invariant
  in exact historical-touch form. Region disjointness implies event-touch
  exclusion directly, while authentic-event touch completeness gives the
  converse under structural well-formedness. The history-level iff keeps the
  ledger, candidate, and current-representative order unchanged. This is an API
  normalization, not unconditional preservation or created-region geometry;
- `SequentialFigure7CrossRepresentativeStablePreservation.lean` now preserves
  the same invariant through the exact prepared prefix and canonical `concl`
  and `nop` history extensions. Remaining ready/waiting work is transported at
  its exact boundary, union-find representatives are unchanged, and an
  output-unmarked tensor mate is shown not to be the newly marked head. The
  prepared helper explicitly requires output-state and complete-ledger
  equalities and does not invent an intermediate history edge. This does not
  cover `new`, `wait`, `forward`, or `unifyPayload`;
- `SequentialFigure7CrossRepresentativeNewPreservation.lean` now proves the
  exact retained/appended-endpoint classification for `new`. The fresh
  reservation event is a maximal self-root and cannot be strictly older than
  any output candidate. Old candidates transport through the stable theorem;
  old events against actual reached/partner candidates require the explicit
  `NewCreatedRegionSeparated` premise. A bounded 16-seed search exercised
  222,246 such candidates and 3,333,924 strict prior-event comparisons without
  an intersection, but this is finite evidence and does not derive the premise
  or prove unconditional New preservation;
- `SequentialFigure7CrossRepresentativeWaitPreservation.lean` now proves the
  exact survivor/inserted-conclusion classification for `wait` and packages
  the introduced tensor candidate without storing the desired separation.
  Prior candidates transport through the stable theorem. The complete
  history extension preserves `OlderSourceRegionSeparated` only under the
  explicit `WaitCreatedRegionSeparated` premise for the new conclusion. A
  bounded 16-seed search exercised 636 such created candidates and 1,068
  strict older-event comparisons without an intersection, but this is finite
  evidence and does not derive the premise or prove unconditional Wait
  preservation;
- `SequentialFigure7CrossRepresentativeForwardPreservation.lean` now proves
  the exact retained/inserted-conclusion classification for `forward` and
  packages the introduced tensor candidate without storing the desired
  separation. Production par queuing preserves marks and representatives, so
  old candidates transport through the stable theorem. The complete history
  extension preserves `OlderSourceRegionSeparated` only under the explicit
  `ForwardCreatedRegionSeparated` premise for the new active-boundary
  conclusion. A bounded 16-seed search exercised 33,582 such candidates and
  117,324 strict older-event comparisons without an intersection, but this is
  finite evidence and does not derive the premise or prove unconditional
  Forward preservation;
- `SequentialFigure7CrossRepresentativeUnifyPayloadPreservation.lean` now
  proves the exact survivor/moved/inserted-conclusion classification for
  arbitrary-payload Unify and the precise representative if-map that redirects
  the retired active class to the previous root. Strict output ordering rules
  out that retired class for every relevant prior event, and moved candidates
  recover their prepared active-boundary witness. The complete history
  extension preserves `OlderSourceRegionSeparated` only under the explicit
  `UnifyPayloadCreatedRegionSeparated` premise for the inserted conclusion. A
  bounded 16-seed search exercised 58,056 such candidates, 243,570 strict
  older-event comparisons, 528,204 retired-event remaps, and 163,806 moved
  candidates without an intersection or decode failure. This is finite
  evidence and does not derive the premise or prove unconditional Unify
  preservation;
- `SequentialFigure7SameRepresentativeEventTouch.lean` now eliminates an
  exact historical reservation-event touch from the selected tensor mate's
  complete source-left region whenever the event and active ready head have
  the same current representative. The proof routes the touch to the event's
  stored-left axiom endpoint, transports that endpoint into the active
  occurrence component, and derives a forbidden reference-switching tensor
  bypass. It uses declarative correctness but no fresh-run premise or success
  equation for an additional current `new?` call. Strictly older events, old
  marked owners, and the four created-candidate side conditions remain open;
- `SequentialFigure7ActiveRegionTouchOrder.lean` now shows that any authentic
  event touch in the active mate region is strictly older than the active head
  in both current representative and immutable raw-age order. With the explicit
  `OlderEventTouchSeparated` hypothesis, every ledger event is touch-separated
  from that region and every region tag is `some false`; the existing
  `OlderSourceRegionSeparated` form is accepted through a compatibility
  theorem. This closes only the conditional historical-tag branch, not global
  separation availability, route/run construction, raw or endpoint readiness,
  queue/capacity obligations, `NewEnabled`, or progress;
- `SequentialFigure7ActiveConclusionTouch.lean` now proves that any authentic
  reservation-event touch of a future candidate tensor conclusion continues
  to the candidate mate or queued head according to tensor orientation. For
  the active candidate, conditional mate-region tag freshness eliminates the
  mate case, including for the same-boundary event, so conclusion touch implies
  head touch. It does not prove the conclusion untouched, eliminate the head
  touch or a raw mark, construct target avoidance, close a created seam, or
  prove enabledness or progress;
- `SequentialFigure7OlderEventFutureWorkTouchSeparation.lean` now names the
  exact strictly older queued-head residue. Combined with
  `OlderEventTouchSeparated` and structural well-formedness, it rules out a
  strictly older event touch of a future tensor conclusion. Empty,
  structurally well-formed init, and Prepared/concl/nop preservation are kernel
  checked. The invariant is not derived from correctness, the scheduler
  invariant, canonical history, or queue provenance. This base module alone
  does not cover a candidate-creating rule; the downstream New theorem closes
  one such case. Wait, Forward, and UnifyPayload now derive their residuals
  structurally before preserving, and a capstone history induction now makes
  queued-head separation globally available. Mate-region and raw-mark
  invariant availability, unconditional stored-left equal-boundary avoidance,
  enabledness, and progress remain open;
- `SequentialFigure7StrictCommitmentTargetAvoidance.lean` now turns the two
  supplied strict separation invariants into retained commitment geometry.
  Under the complete scheduler invariant, an adjacent edge whose child boundary
  is strictly older than a future candidate automatically receives the exact
  child-event untouched callback and a canonical target-avoiding path. Strict
  sigma ordering lifts this to every positive retained interval from strict
  oldness of only its final boundary. It does not cover an equal final boundary,
  derive either invariant globally, recover queue origin, close a raw seam, or
  establish enabledness, progress, completeness, fallback removal, or
  complexity;
- `SequentialFigure7StrictOlderSigmaSplit.lean` now locates that geometry for
  an arbitrary authentic ledger event whose current representative is strictly
  older than a future-New candidate. It returns the event representative's
  exact retained position, the candidate's immediate predecessor, and the
  possibly empty prefix between them. The consumer composes every positive
  prefix and handles zero explicitly. The final predecessor-to-candidate edge,
  global separation availability, queue origin, created-head/raw seams,
  enabledness, progress, completeness, fallback removal, and complexity remain
  outside the theorem;
- `SequentialFigure7EqualBoundaryCommitmentTargetAvoidance.lean` now
  classifies that final edge. Stored-right orientation yields the canonical path
  avoiding the active tensor conclusion. The general inclusive dichotomy yields
  that path or an authentic same-age stored-left child event with the exact
  adjacent conclusion-to-head trace fragment. The second branch witnesses
  failure of the generic child-untouched callback, not absence of every avoiding
  path, and both branches may hold. This does not derive mate-region/raw-mark
  invariants, queue origin, progress, totality, completeness, fallback removal,
  scheduling, or complexity;
- `SequentialFigure7CommitmentBlockerAdvance.lean` now combines queued-head
  separation, the strict `sigma` split, strict target avoidance, and the final
  equal-boundary classification. Under declarative correctness and the complete
  scheduler invariant, for a supplied canonical history, active `NewGuard`, and
  authentic ledger event whose current representative is strictly below the
  active head, it yields an exact avoiding path, a mate-touching ledger event at
  a strictly higher representative still below the head, or the exact equal
  stored-left callback-failure trace. At that checkpoint the alternatives were
  inclusive; representative advance was not chronology and was not yet
  maximalized or eliminated, while callback failure did not deny path
  existence. No mate-
  region invariant, created-candidate raw-seam discharge, global raw-mark
  invariant, enabledness, progress, completeness, fallback-removal, scheduling,
  or complexity claim follows;
- `SequentialFigure7CommitmentBlockerMaximality.lean` eliminates that
  intermediate current-representative advance under the same explicit public
  inputs. It filters the finite authentic mate-touch blockers above the original
  event and chooses a maximum representative. An avoiding commitment path for
  that blocker would splice into the tensor bypass forbidden by reference-
  switching acyclicity, while another advance contradicts maximality. The
  inclusive result is therefore an exact avoiding path or the exact equal
  stored-left callback failure. The callback branch does not deny path
  existence. Queue origin, the remaining mate-region/global raw-mark
  invariants, created-candidate raw seams, enabledness, progress, completeness,
  fallback removal, scheduling, and complexity remain open;
- `SequentialFigure7ActiveRegionTouchSeparation.lean` closes the local
  active-mate touch obstruction without promoting a global separation
  invariant. Its public `ActiveMateEventAnchor` is an exact reference path from
  the active mate to an authentic event's stored-left axiom endpoint that avoids
  the active tensor conclusion. For a strictly older event, the maximality
  theorem's path branch combines with that anchor and exact component geometry
  to form the forbidden active-tensor bypass. In the equal-boundary stored-left
  callback branch, the anchor, retained prefix, final New edge, and callback
  trace instead form an alternate exact mate-to-conclusion walk that omits the
  active tensor edge, contradicting reference-tree uniqueness. Hence no
  strictly older authentic event carries an anchor. Any event touch inside the
  complete active mate source-left region constructs such an anchor and is
  therefore impossible. This conclusion is indexed by one supplied active
  guard; it neither says that callback failure is impossible without an anchor
  nor constructs a history, global invariant, or dispatcher step;
- `SequentialFigure7OlderEventFutureWorkTouchNewPreservation.lean` now
  preserves the queued-head invariant through every already-successful typed
  New step from a supplied prior canonical history and invariant. Retained
  candidates transport through Prepared, old-event touches of created
  reached/partner endpoints contradict canonical touch disjointness, and the
  fresh event cannot be strictly older. No created-region premise is required.
  This is not a global-availability, same-boundary, Wait/Forward/UnifyPayload,
  raw-seam, enabledness, or progress theorem;
- `SequentialFigure7OlderEventFutureWorkTouchWaitPreservation.lean` now
  preserves the queued-head invariant through an already-successful typed Wait
  step when the candidate-indexed `WaitCreatedHeadTouchSeparated` residual is
  supplied. Retained candidates transport through the destination and Prepared
  state, an actual inserted candidate is exactly the residual case, and Wait
  appends no ledger event. Relative to the supplied prior invariant this is the
  exact transition-local obligation, but no scheduler/history/reachability
  theorem derives it. This is not unconditional/global Wait, Forward or
  UnifyPayload preservation, same-boundary exclusion, raw-seam discharge,
  enabledness, or progress;
- `SequentialFigure7OlderEventFutureWorkTouchWaitDischarge.lean` now derives
  that Wait created-head residual under structural well-formedness. The exact
  submitted par decomposes a hypothetical touch to the stored-left premise,
  which is either the selected occurrence or its already-marked mate. Exact
  reservation realization and live-component disjointness rule out both
  orientations. The direct corollary still consumes the supplied prior
  invariant and an already-successful typed Wait step. It does not close
  `WaitCreatedRegionSeparated`, the raw seam, the final equal-boundary
  callback, global availability, enabledness, or progress;
- `SequentialFigure7OlderEventFutureWorkTouchForwardPreservation.lean` now
  preserves the queued-head invariant through an already-successful typed
  Forward step when the candidate-indexed
  `ForwardCreatedHeadTouchSeparated` residual is supplied. Retained candidates
  transport through Prepared and exact Forward representative equality, an
  actual inserted candidate is exactly the residual case, and Forward appends
  no ledger event. Relative to the supplied prior invariant this is the exact
  transition-local obligation, but no scheduler/history/reachability theorem
  derives it. This is not unconditional/global Forward or UnifyPayload
  preservation, same-boundary exclusion, raw-seam discharge, enabledness, or
  progress;
- `SequentialFigure7OlderEventFutureWorkTouchForwardDischarge.lean` now
  derives that Forward created-head residual under structural well-formedness.
  Exact ledger endpoint accounting places the old event endpoint in its
  representative's owned carrier; a hypothetical touch of the inserted
  Forward conclusion and occurrence-carrier source-left closure place the same
  endpoint in the active par carrier. Strict representative order and live
  component disjointness contradict the overlap. The direct corollary still
  consumes the supplied prior invariant and an already-successful typed
  Forward step. It does not close `ForwardCreatedRegionSeparated`, the raw
  seam, the final equal-boundary callback, global availability, enabledness, or
  progress;
- `SequentialFigure7OlderEventFutureWorkTouchUnifyPayloadPreservation.lean`
  now preserves the queued-head invariant through an already-successful typed
  UnifyPayload step when the candidate-indexed
  `UnifyPayloadCreatedHeadTouchSeparated` residual is supplied. Survivors reuse
  the prior invariant, moved work is recovered at the prepared active boundary,
  and strict output order excludes an older event from the retired active
  representative class. An actual inserted tensor candidate is exactly the
  residual case, and UnifyPayload appends no ledger event. Relative to the
  supplied prior invariant this is the exact transition-local obligation, but
  no scheduler/history/reachability theorem derives it. This is not
  unconditional/global UnifyPayload preservation, same-boundary exclusion,
  raw-seam discharge, enabledness, or progress;
- `SequentialFigure7OlderEventFutureWorkTouchUnifyPayloadDischarge.lean` now
  derives that UnifyPayload created-head residual under structural
  well-formedness. The exact tensor output joins the previous and active live
  occurrence carriers. A hypothetical old-event touch sends the event's
  stored-left axiom endpoint into that output carrier, while chronological
  realization keeps it in the strictly older event carrier; exact forest
  disjointness rules out the overlap in both tensor orientations. The direct
  corollary still consumes the supplied prior invariant and an
  already-successful typed UnifyPayload step. It does not close
  `UnifyPayloadCreatedRegionSeparated`, the raw seam, the final equal-boundary
  callback, global availability, enabledness, or progress;
- `SequentialFigure7OlderEventFutureWorkTouchAvailability.lean` now closes
  global availability for the queued-head invariant. A direct induction over
  every supplied canonical history uses the empty and structurally well-formed
  initialization cases plus all six successful-rule preservation theorems.
  This classifies existing proof-carrying histories; it does not create one,
  enlarge reachability, derive mate-region or raw-mark separation, supply
  unconditional stored-left equal-boundary avoidance, or prove enabledness,
  progress, totality,
  completeness, fallback removal, scheduling, or complexity;
- `SequentialFigure7ActiveRegionAvailability.lean` now applies the complete
  structural run-or-blocker search to that tag-fresh active region. It produces
  either a complete `NewSourceRegionInput` or an exact old marked occurrence
  owner, and then either `NewEnabled` or that owner; a pointwise no-owner
  premise closes `NewEnabled`. Thus route/run, raw and endpoint readiness,
  endpoint queue absence, fresh waiting capacity, and the future-cell premise
  are no longer separate obligations on the successful branch. The module does
  not prove global older-event separation, eliminate the owner, discharge the
  four created-candidate side conditions, or prove progress;
- `SequentialFigure7OlderRawMarkedRegionSeparation.lean` now defines the
  state-only raw-mark separation invariant needed by that owner branch. It is
  established for empty and initialized states, preserved through
  Prepared/concl/nop, and under correctness plus the complete scheduler
  invariant it excludes every raw mark and exact marked owner from the active
  mate region. Combined with the preceding availability theorem, this yields
  `NewEnabled` whenever both the event-touch and raw-mark separation invariants
  are supplied. It does not by itself preserve the raw invariant through a
  candidate-creating rule;
- `SequentialFigure7ActiveRegionEnabledness.lean` removes the local active-
  guard dependency on both global separation invariants. For a concrete raw
  mark in the active mate region, the complete scheduler invariant and raw-mark
  reservation anchor recover the same-age authentic event and an exact path
  inside its final owned component. The active region path and owned path splice
  into an `ActiveMateEventAnchor`; representative order and the preceding local
  theorem rule it out. Thus the region has no concrete raw mark or exact marked
  owner, while canonical touch provenance makes every region tag false. The
  structural run-or-blocker theorem then yields `NewSourceRegionInput`, whose
  established queue, capacity, and future-cell bridges yield input-only
  `NewEnabled`. The theorem assumes an already established canonical history
  and active `NewGuard`; it does not construct either one, choose or execute a
  dispatcher branch, prove branch exhaustiveness or progress, globalize or
  preserve raw separation through New/Wait/Forward/UnifyPayload, remove the
  recursive fallback, or establish token-age scheduling or linearity;
- `SequentialFigure7ReadyHeadDispatchResidual.lean` composes the complete
  ready-head case analysis with the stable predicates, the preceding active
  `NewEnabled` theorem, marked-tensor adjacency, and fixed priority. For a
  supplied correct canonical-history `ReadyHeadInput`, it proves the inclusive
  disjunction of an existential `PriorityEnabled` branch and
  `ReadyHeadMarkedTensorPredecessorGap`. The gap retains an exact marked tensor
  whose mate resolves to a retained boundary strictly below the active top but
  has no input-only immediate-predecessor witness. The reachable wrapper turns
  the positive branch into an exact `dispatch?` result. The theorem neither
  makes the alternatives exclusive nor eliminates the gap without an additional
  invariant;
- `SequentialFigure7OlderMarkedTensorPredecessorInvariant.lean` defines that
  invariant over every ready or waiting `FutureWorkAt`, not only the selected
  head. A marked tensor mate whose current representative is strictly below the
  work's current representative carries an indexed adjacent pair in sigma
  together with its exact boundary lookup. The ready-head projection
  turns this carrier into `SigmaPredecessorInput` and therefore contradicts the
  residual's no-predecessor field. Empty and the initial-reservation step
  establish the invariant; Prepared transports it while excluding the newly selected mark as
  a strictly older mate of retained work; `concl` and `nop` reuse that prefix;
- `SequentialFigure7OlderMarkedTensorPredecessorNewPreservation.lean` closes the
  direct canonical New branch. Retained work transports across the fresh sigma
  append. For a created reached/partner endpoint whose tensor mate was already
  marked, canonical reservation geometry forces the mate boundary to the old
  active top, immediately before the appended fresh boundary. The proof keeps
  that semantic seam private. That module does not itself provide `wait`,
  `forward`, or `unifyPayload` preservation or package a full history;
- `SequentialFigure7OlderMarkedTensorPredecessorWaitPreservation.lean` closes
  the canonical successful Wait branch. Retained work transports through the
  prepared prefix and the sigma-preserving destination update. For the inserted
  conclusion, private reference-switching, touch-separation, and commitment
  geometry constructs the exact adjacent sigma predecessor in both par
  orientations. The theorem requires a supplied prior invariant, complete
  scheduler invariant, declarative correctness, canonical history, and typed
  Wait dispatch and `WaitStep` witnesses. It does not package full-history
  availability. That module now also exposes
  `CanonicalTagHistory.markedMate_sigmaImmediatePredecessor_of_childAnchor` as
  a conditional support bridge. It consumes strict older-event separation and
  an exact child-event anchor and proves neither premise, branch applicability,
  nor dispatcher progress;
- `SequentialFigure7OlderMarkedTensorPredecessorForwardPreservation.lean`
  closes the canonical successful Forward branch. Retained work transports
  through the prepared prefix. For the inserted conclusion, private Forward
  history, component, and touch geometry supplies the bridge premises. The
  public preservation theorem requires a supplied prior invariant, complete
  scheduler invariant, declarative correctness, canonical history, typed
  Forward dispatch, and `ForwardStep`; it does not manufacture any of them or
  package full-history availability;
- `SequentialFigure7OlderMarkedTensorPredecessorUnifyPayloadPreservation.lean`
  closes the canonical successful UnifyPayload branch. The supporting
  `UnifyPayloadStep.createdConclusionTouchSeparated` theorem is the raw,
  carrier-free form of the inserted-conclusion touch result: it assumes
  structural well-formedness, canonical history, and an already-successful
  typed step, but not the future-candidate carrier used by the wrapper.
  Retained predecessor evidence transports across the final sigma pop. A moved
  active candidate would force its marked mate to resolve both to and strictly
  below the surviving boundary, so that case is impossible. For the created
  tensor conclusion, final component provenance constructs the child anchor,
  and the raw touch theorem supplies the strict older-event separation consumed
  by the public conditional bridge. The preservation theorem requires the
  supplied prior invariant, complete scheduler invariant, declarative
  correctness, canonical history, typed Unify dispatch, and
  `UnifyPayloadStep`; it does not manufacture any of them. The successful-rule
  branch prefix is closed through UnifyPayload, but this transition theorem
  alone does not package complete history availability;
- `SequentialFigure7OlderMarkedTensorPredecessorHistory.lean` performs that
  packaging by induction over `CanonicalTagHistory`. Empty and initialization
  use the base results; every later case splits the exact dispatcher evidence,
  reconstructs its typed successful step, and invokes the matching
  `concl`/`nop`/`new`/`wait`/`forward`/`unifyPayload` preservation theorem.
  `ExecutedHistory.olderMarkedTensorPredecessorInvariant` exposes the invariant
  for every executed history under declarative correctness. The reachable
  wrapper combines it with the ready-head residual theorem: a positive priority
  branch yields an exact `dispatch?` result, while the gap contradicts the
  indexed immediate predecessor. `ReadyHeadInput` remains an explicit premise.
  The module proves neither semantic nonterminal-to-ready-head existence nor
  unconditional dispatcher progress, later-state totality, global raw seams,
  fallback removal, sequentialization, faithful scheduling, Figure-7
  pure-worklist completeness, or linearity;
- `SequentialFigure7ActiveTopResidual.lean` isolates the exact post-history
  ready-head structural residual. `ActiveTopDrained` records the last sigma
  boundary, its
  live component, and absence of raw-unmarked occurrences on that frontier.
  Under the complete scheduler invariant and `0 < nextAge`, the last ready
  bucket is exactly that raw-unmarked frontier, so no `ReadyHeadInput` exists
  iff the active top is drained. The reachable wrapper then returns the
  non-exclusive disjunction of an exact dispatcher result and the drained
  witness. It deliberately does not identify drained alone with fully marked or
  terminal;
- `SequentialFigure7ActiveTopMarkedNonconclusionDebt.lean` defines the state
  predicate that pairs every marked active-frontier nonconclusion with a
  raw-unmarked active-frontier nonconclusion. Empty and initial reservation
  establish it. New establishes it without an additional scheduler-invariant
  premise, Concl preserves a prior instance, and Forward/UnifyPayload establish
  it under the prior complete scheduler invariant when their created conclusion
  is not global. Under declarative correctness and the complete scheduler
  invariant, the predicate plus `ActiveTopDrained` implies
  `core.allMarked = true`. This is the conditional semantic reduction;
  complete canonical-history preservation of that debt remains open;
- `SequentialFigure7ActiveTopDebtBranchResidual.lean` makes the four local
  obligations exact. Under prior debt, post-Nop and post-Wait debt are
  equivalent to the prepared selected-away witness. Under the prior complete
  invariant and a global created conclusion, Forward and UnifyPayload post-debt
  are equivalent to presence implying a non-global vertex in the exact ready
  tail. Presence only detects vacuity; canonical history derives no debt
  witness, so the module proves no full debt preservation or progress;
- `SequentialFigure7ActiveTopDebtQueueTail.lean` normalizes the Nop and Wait
  boundaries further. Under prior debt and the input `SchedulerInvariant`,
  post-step debt is equivalent to a non-global vertex in the prepared
  `remainingTop`. The iff theorems expose but do not manufacture that witness;
- `SequentialFigure7ActiveTopDebtHistoryTail.lean` packages the exact six-case
  obligations as the reset-aware `ActiveTopDebtTailLaw`. Concl, Nop, and Wait
  recurse; Nop and Wait require the exact current remaining-tail witness. New
  resets, and Forward/UnifyPayload reset recursion to their exact current
  non-global-created or global-created-tail obligation. Supplying the law with
  its canonical tag history implies endpoint debt. Correctness, canonical
  history, and reachability do not yet derive the law, so no unconditional
  `allMarked`, progress, termination, totality, or completeness result follows;
- `SequentialFigure7ActiveTopDebtParentEscape.lean` gives a separate bounded
  first-boundary reduction that neither assumes nor derives that tail law. For
  an explicit ready head whose `ConnectiveBelow` witness is a `par`, declarative
  correctness plus `SchedulerInvariant` exposes the active component
  occurrence/accounting receipt and either a non-global ready-tail witness or
  `ActiveCarrierParentEscape`. The escape contains a concrete marked non-global
  frontier premise distinct from the selected vertex whose exact submitted
  connective parent conclusion lies outside the active owned carrier. The
  theorem does not assert that the outcomes are exclusive. The
  failure-conditioned theorem forces escape when the
  tail witness is absent. A supplied `CanonicalTagHistory` only authenticates
  the concrete mark as an earlier prepared-selection event. The public
  checkpoint contains no computational coexistence receipt. The downstream
  re-entry classifier identifies exact submitted-parent target status; its
  stored-right no-tail specialization eliminates the selected target, while
  the concretely marked target remains open;
- `SequentialFigure7ActiveTopDebtParentEscapeTemporal.lean` normalizes that
  no-tail escape by the escaped source kind. The par branch aligns the mark to
  its authentic reservation event and classifies the parent continuation as a
  raw sibling, strictly older future work, or a strictly older marked
  conclusion. The tensor branch aligns the mark to the active representative,
  places its sibling and parent conclusion outside the active carrier, and
  retains the older marked-tensor predecessor invariant without fabricating
  its strict-order trigger. The combined result is a residual classification,
  not a tail witness or impossibility theorem, and it neither assumes nor
  derives `ActiveTopDebtTailLaw`;
- `SequentialFigure7ActiveTopDebtParentTemporalOutcome.lean` consumes the
  canonical continuation-credit result for the tensor same-boundary case and
  maps both source branches to one endpoint carrier. Its cases are a selected
  or external raw sibling, an external future parent at a strictly older
  boundary, or an external marked parent at a strictly older representative.
  The marked outcome may continue, so this is not a terminal-exit claim. The
  module adds no tail witness, history-tail proof, residual elimination, or
  progress result;
- `SequentialFigure7ActiveTopDebtParentExternalTemporalOutcome.lean`
  specializes the common outcome to actual no-tail Nop and Wait steps. Their
  typed mate facts exclude the selected raw endpoint. The remaining carrier is
  external raw work, external future work at a strictly older boundary, or an
  external marked parent at a strictly older representative. External means
  outside the active owned carrier; no re-entry, tail witness, tail law, or
  progress result follows;
- `SequentialFigure7ActiveTopDebtParentExternalCommitmentOutcome.lean`
  places both strictly older external branches on an exact retained `sigma`
  interval and retains the final canonical commitment edge into the active
  top. The external raw branch is unchanged. The carrier supplies no path from
  the external endpoint to that edge, first re-entry, ready-tail witness,
  history-tail law, or progress result;
- `SequentialFigure7ActiveTopDebtParentExternalEndpointCrossing.lean`
  uses the active child occurrence to construct an exact reference-switching
  path from inside the active owned carrier to each older external endpoint and
  retains one exact stored-edge owned-to-outside crossing. The external raw branch is
  unchanged. The crossing is not yet classified as a distinct raw payer,
  re-entry, history-tail law, or progress result;
- `SequentialFigure7ActiveTopDebtParentExternalCommitmentReentry.lean`
  composes every adjacent canonical commitment path across the positive
  retained interval. Ready future and older marked endpoints gain an exact
  endpoint-to-active path and outside-to-inside re-entry edge. Waiting retains
  its exact cell, and external raw work stays unchanged. The re-entry edge is
  not classified as a distinct payer, history-tail law, or progress result;
- `SequentialFigure7ActiveTopDebtParentExternalReentryTarget.lean` identifies
  that edge as the reverse of an exact submitted connective-parent edge whose
  target is a non-global active-frontier premise. At a ready head, exact
  ready/frontier accounting classifies it as the selected raw head, a raw
  ready-tail occurrence, or a prior concrete mark. Explicit no-tail removes
  only the middle case. Canonical raw-mark history authenticates the marked
  target at the active representative. If the retained path also avoids the
  current par conclusion, parent-link uniqueness removes the selected target
  and leaves a distinct marked premise. An explicit child-event untouched law
  now yields par-conclusion avoidance for one adjacent commitment edge. At the
  active edge, an inclusive dichotomy returns avoidance or an authentic
  same-age trace step from the par conclusion to selected or mate. The complete
  positive retained interval now similarly yields a composed avoiding endpoint
  path or one exact failed child edge with an authentic selected/mate trace;
  the child is strictly before or equal to the final boundary and the outer
  alternatives remain inclusive. Active-carrier localization rules out
  strictly older selected and active-owned-mate traces, leaving only a
  stored-right trace to an external mate. Actual Nop makes that endpoint
  raw-unmarked; actual Wait gives its exact concrete mark and a strictly older
  representative. Supplied reference-switching connectedness now returns that
  mate through an exact active-carrier inbound parent edge and classifies the
  target as selected raw, non-global ready-tail raw, or concretely marked.
  Under exact no-tail failure, the strictly older stored-right branch cannot
  target the selected head; it leaves a distinct authenticated concrete mark
  represented at the active boundary. Equal-final selected/mate traces remain.
  Retained-path simplicity makes that exact mark distinct from the starting
  current mate, and structural parent uniqueness shows that every connective
  view rooted at it has a mate different from the selected head. This excludes
  the target's selected raw-sibling alternative without eliminating the target
  or choosing its submitted source kind. The unique consumer at that target is
  now bound to the exact inbound edge, and canonical continuation credit gives
  a target-indexed trichotomy: raw mate outside the active carrier, queued
  parent work at a strictly older boundary, or marked parent conclusion at a
  strictly older representative. A finite continuation normalization now
  follows the marked non-global case to raw work outside, an exact raw return
  to the current selected/mate pair, older future work, or an older marked
  global conclusion. None of those cases is eliminated.
  The exact raw return now has a full-graph cyclic normal form. Its retained
  switching prefix and forward continuation tail are each nonbacktracking.
  Complete cancellation is empty or localized to one of their two oriented
  endpoint junctions. Both segments then have unique occurrence indices; every
  prefix occurrence is backward, has its exact reverse in the tail, and reaches
  a concrete marked nonconclusion. Source uniqueness for the retained simple
  path orders the tail as its exact reverse traversal.
  A nonempty remainder exposes a par pair, with the kept occurrence in the
  prefix and the omitted occurrence in the tail; the latter starts at a
  concrete marked nonconclusion. Both outcomes remain residuals.
  For a successful typed Nop, the exact raw return is now eliminated: every
  nontrivial marked-conclusion chain ends concretely marked, while the current
  opposite premise remains raw-unmarked. The refined Nop target retains
  raw-outside, older-future, and older-marked-global exits. Wait keeps the exact
  return and cyclic residuals. The exact Wait return is now refined further:
  its first marked parent conclusion outside the active carrier is an
  authenticated canonical-history event at a strictly older representative.
  Canonical history now also orders authentic prepared-selection raw-mark
  events. Both submitted premises precede an authentically marked connective
  conclusion, so the Wait descent records that its origin and sibling mate
  precede the older parent conclusion while retaining the sibling continuation
  exit. The strict relation is now transitive and asymmetric. Every distinct
  marked-chain origin precedes an authenticated terminal, and the Wait target
  authenticates the outer mate as that terminal. In the retained causal branch,
  the re-entry origin and first sibling precede the complete chain endpoint.
  Authentic marks at distinct vertices are now totally ordered, with a full
  equality-or-two-orders comparison. The first sibling's raw and future exits
  remain unchanged, while its marked-global endpoint is classified strictly
  before or after the non-global outer terminal. Neither order is eliminated.
  The complete marked-conclusion chain retained by that first descent now also
  carries its exact cyclic-junction normal form on the same switching path and
  target connective. This aligns the sibling chronology and switching geometry
  without eliminating either witness. Nonempty complete cancellation now
  exposes both reverse endpoint junctions simultaneously, and its cyclic source
  is equal to or strictly before the authenticated outer terminal. Exact
  retained-prefix index uniqueness now rules out the equality case: otherwise
  mask retention would produce a forbidden nonempty closed cyclically
  nonbacktracking walk in the correct reference-switching tree. Both junctions
  remain while the cyclic source is strictly earlier. Elimination of the
  descent, sibling
  exit, remaining marked-target and
  equal-final cases, the history-tail law, and progress remain open;
- `SequentialFigure7ContinuationCredit.lean` and
  `SequentialFigure7ContinuationCreditPreservation.lean` close a weaker
  full-history invariant. Their two carriers assign every concretely marked
  nonconclusion an unmarked-mate, future-conclusion, or marked-conclusion
  receipt. Fresh events receive credit in all six cases. The six branch
  transports and two dispatcher transports require structural well-formedness;
  Nop and New additionally consume the old owner's concrete mark, while the
  old-credit dispatcher theorem takes that mark uniformly. Exact canonical tag
  histories preserve the predicate without a correctness premise. This gives
  no active-frontier raw witness and therefore no selected-away or exact-tail
  discharge, full active-top debt preservation, or progress;
- `SequentialFigure7ContinuationExit.lean` normalizes every supplied
  continuation receipt through a finite chain of marked non-global conclusions.
  Formula complexity strictly increases at each step, and the three exits are
  an unmarked raw mate, future-conclusion work, or a marked global conclusion.
  A separate endpoint-bound carrier has only raw and future cases, binds that
  endpoint to one component frontier, and has no global constructor. Its
  active-top locality law is sufficient, is not claimed necessary, and is not
  derived from canonical history. With structural well-formedness and queued
  vertices unmarked it implies active-top debt; with declarative correctness,
  the complete scheduler invariant, `ActiveTopDrained`, and locality it implies
  `core.allMarked = true`. This is no arbitrary history or locality existence,
  unconditional progress, completion, terminality, or totality theorem;
- `SequentialFigure7EndpointLocalityObstruction.lean` proves that every
  successful typed `WaitStep` from a scheduler-invariant input refutes
  `ActiveTopContinuationExitLocalized` at its output. The unrestricted
  same-component law therefore cannot be a full canonical-history invariant
  across successful Wait transitions. The earlier locality-to-debt and
  locality-to-`allMarked` implications remain valid when their hypothesis is
  supplied. The obstruction proves neither post-Wait drainedness nor
  reachable-Wait existence or progress, and it does not refute direct debt or a
  Wait-compatible drained, temporal, or cross-component weakening. The concrete
  `native_decide` trace remains research-only and outside the public theorem;
- `SequentialFigure7OlderRawMarkedRegionNewPreservation.lean` now preserves
  the raw-mark invariant through a successful typed New step, conditional on
  `NewRetainedRawMarksSeparated`. The selected new mark is excluded from every
  created endpoint region by reference-switching acyclicity, while retained
  candidates use Prepared preservation. The condition contains exactly the
  remaining input-retained-mark/created-region seam; it is not derived from
  canonical history or reachability. This New-layer theorem alone does not
  establish global availability or progress;
- `SequentialFigure7OlderRawMarkedRegionWaitPreservation.lean` now preserves
  the raw-mark invariant through a successful typed Wait step, conditional on
  `WaitRetainedRawMarksSeparated`. Exact Wait age order excludes the selected
  mark from a created destination candidate, while retained candidates use
  Prepared preservation. The condition contains exactly the remaining
  input-retained-mark/created-region seam. It is distinct from
  `WaitCreatedRegionSeparated`, requires no correctness/history witness, and is
  not derived from canonical reachability. It does not prove unconditional
  Wait, enabledness, or progress;
- `SequentialFigure7OlderRawMarkedRegionForwardPreservation.lean` now
  preserves the raw-mark invariant through a successful typed Forward step,
  conditional on `ForwardRetainedRawMarksSeparated`. The selected mark and
  inserted candidate share the exact active raw age, while retained candidates
  use Prepared preservation. The condition contains exactly the remaining
  input-retained-mark/created-region seam. It is distinct from
  `ForwardCreatedRegionSeparated`, requires no correctness/history witness,
  and is not derived from canonical reachability. It does not prove
  unconditional Forward, enabledness, or progress;
- `SequentialFigure7OlderRawMarkedRegionUnifyPayloadPreservation.lean` now
  preserves the raw-mark invariant through a successful typed arbitrary-
  payload Unify step, conditional on
  `UnifyPayloadCreatedRawMarksSeparated`. Strict output order excludes raw
  marks from the retired active representative class, while survivor and moved
  candidates use the prepared invariant. The condition contains exactly the
  remaining prepared-mark/inserted-candidate seam. Its candidate origins cover
  the output but need not be exclusive. It is distinct from
  `UnifyPayloadCreatedRegionSeparated`, requires no correctness/history
  witness, and is not derived from canonical reachability. It does not prove
  unconditional Unify, enabledness, or progress;
- next establish both separation invariants for the relevant correct canonical
  histories by discharging the genuinely new candidate obligations created by
  `new`, `wait`, `forward`, and `unify`. Do this without assuming historical
  touch and ownership provenance classes are globally disjoint, or that every
  historical touch is owned by its reserving component. The converse,
  run-indexed separation is
  now isolated in `SequentialFigure7RegionBoundaries.lean`; it consumes the
  run and is not the missing construction theorem. Use the resulting run to
  derive exhaustive enabledness for correct
  certified-reachable nonterminal states and then an exhaustive full-rule
  reachable-state/progress theorem under correctness. The classification itself
  is not `NewGuard` sufficiency, progress, totality, completeness, fallback
  removal, or linearity;
- keep the existing `dynamicStartWithFuel?` as an independent eager Figure-5
  refinement only. Figures 7–8 `init`/`new` delay endpoint marking by pushing
  endpoints into `R`;
- state scheduler guards over raw assigned token ages, not their union-find
  representatives;
- prove the special ordered union-find representation correct;
- count every vertex visit, queue/stack operation, representative operation,
  and final verification cost.

The library may claim Guerrini-style whole-program linearity only after this
separate cost theorem is kernel checked for the actual executable.

The stable enabled-input field audit is intentionally exact:

| Input | Stored fields |
| --- | --- |
| `ReadyHeadInput` | `vertex`, `readyTail`, `rawAge`, `top_ready`, `sigma_top` |
| `ConclInput` | `head`, conclusion-boundary proof |
| `SubmittedParInput` | submitted link index/endpoints/conclusion, selected side, exact link and premise equations |
| `NopInput` | `head`, `par`, mate-unmarked proof |
| `WaitInput` | `head`, `par`, `mateRawAge`, mate-marked proof, strict older-age proof |
| `ForwardInput` | `head`, `par`, `mateRawAge`, mate-marked proof, non-older proof |

No row stores an output state, executor-success equation, derived wait
destination/payload, derived forward component/occurrence pick, or final-ready
`Nodup`. Those are consequences of the complete `SchedulerInvariant`, not
applicability inputs.

### Rejected flat-scheduler invariant and proof-plan split

The current flat eager scheduler cannot be assigned the Figure-7
token-age/LIFO invariant. A stable accepted certificate with three axioms and
two tensors starts token ages 0, 1, and 2 in submitted link order. Because the
initial connective queue is reversed, its first tensor firing joins ages 0 and
2; the complete worklist run succeeds in two attempts with zero waiting
requeues and two firings. The counters are only the public regression receipt:
the noncontiguous class follows from the fixed links plus the eager-start and
reverse-queue definitions.

Similarly, `tagSchedulerFamily.step` numbers a selected dependency-cycle
segment rather than worklist time or token age. The terminal order
`firstTag → lastTag → anchor → outerLast` gives separated endpoint pairs, and
ordinary laminarity permits them as siblings. It cannot by itself establish
that the alleged outer pair is an operational ancestor containing the inner
pair.

The v0.10 proof plan therefore splits. Two initially plausible confluence
relations are already too fine: exact concrete-state confluence fails on a
derivation-generated correct certificate, and structural-only confluence fails
on a structurally well-formed certificate. Current flat-worklist completeness
may instead be pursued by proving that `ResidualParsingWitness` is preserved
by every successful competing firing, or by a local-confluence theorem modulo
the candidate observation

```text
(marked occurrence domain, occurrence-thread partition).
```

The two exploratory counterexamples use the following exact incidence lists
(atomic occurrences are paired by the displayed axioms and the remaining
occurrences are the displayed tensor conclusions):

```text
correct, exact-state counterexample
axiom 0 1
axiom 2 3
axiom 4 5
tensor 0 2 6
tensor 1 4 7
conclusions [7, 6, 3, 5]

structural-only counterexample
axiom 0 1
axiom 2 3
tensor 0 2 4
tensor 1 3 5
conclusions [4, 5]
```

In the first certificate both tensor orders terminate successfully but retain
different raw union/derivation construction histories; in the second,
whichever tensor fires first disables the other. These fixtures are recorded
here as counterexample inputs, not as a committed executable regression.

An uncommitted exploratory local audit of 80 derivation-generated correct
certificates visited 2,734 reachable quotient states and 7,148 enabled
critical pairs, with 0 disabled pairs and 0 quotient non-diamonds. No committed
script or artifact currently reproduces those counts, so they are not a
release or CI gate. This is not a local-diamond, confluence, progress,
pure-worklist-completeness, or complexity theorem. Guerrini-style linearity
instead requires extending the separate bounded/tagged `NEXTAXIOM` checkpoint
through the proved initial production bridge and faithful full `R`/`W`
token-age sequencing; the independent raw-age `σ`/waiting state plus one
reservation is still only the first slice. Neither route
changes the already complete general checker-accepted sequentialization
theorem. Canonicalization likewise remains exact only for
`ProofNetEquivalent`, which preserves ordered conclusions; it is not arbitrary
unlabeled graph isomorphism.

## Release gates

- [x] independent proof-irrelevant marking state and Figure-5 step relation;
- [x] raw token identity separated from semantic thread equivalence;
- [x] executable-to-abstract state map under an explicit bounds contract;
- [x] representative-range validity made an explicit abstraction precondition;
- [x] executable mark-array/domain alignment made an explicit precondition;
- [x] representative idempotence made explicit and conclusion marking proved
  to preserve the abstraction contract;
- [x] concrete conclusion marking proved to refine the independent forward
  rule under the exact par guards;
- [x] representative lookups connected back to allocated raw marks and their
  semantic thread without additional axioms;
- [x] fixed-carrier fresh-token semantics proved globally equivalent, with
  the newly exposed token isolated from every previously allocated token;
- [x] identity-parent start preservation and exact abstract `start` refinement
  for the real `startAxiom?` execution path;
- [x] successful real `firePar?` execution, including component construction,
  proved to refine one independent forward step;
- [x] `MergeExtension` proved to preserve equivalence;
- [x] cyclic-parent counterexample recorded during development and excluded by
  an ordered-forest invariant;
- [x] ordered parents proved to imply representative range, sufficient-fuel
  stability, and representative idempotence;
- [x] ordered parents preserved through axiom initialization, par/tensor
  firings, eager passes, and saturation prefixes;
- [x] tensor guards factored into `unifyTokens?`, with concrete
  `mergeConclusion` bounds/forest preservation and real-firing observation
  equivalence;
- [x] exact representative-update theorem equating `mergeConclusion` with
  `MergeExtension` on the full fixed carrier;
- [x] eager connective firing, pass, and finite-saturation abstraction
  preservation plus independent execution refinement;
- [x] whole successful initialization-plus-saturation reachable-state
  execution theorem from the empty marking;
- [x] partial-derivation formula/frontier invariant for axiom, par, tensor,
  complete passes, saturation prefixes, and the whole successful eager run;
- [ ] scheduler coverage invariant;
  - [x] operational queued/fired/idle/waiting/deadlock classification and
    initial-connective coverage;
  - [x] exact no-missed-dependency direction for the precomputed consumer
    table;
  - [x] sound deduplication flags plus coverage preservation for ordinary
    enqueues, dependency fan-out, waiting registration, and full
    waiting-par requeue;
  - [x] bounded queue/waiting registries, sound waiting deduplication, and
    pop preservation after clearing the exact head flag;
  - [x] pending-link premise/frontier coverage stated without incorrectly
    requiring consumed marked occurrences to remain exposed, plus exact
    one- and two-occurrence frontier-picker totality;
  - [x] guard-ready par/tensor component construction proved total under that
    invariant, and the processed popped connective reclassified as fired,
    idle, registered waiting, or tensor-deadlocked;
  - [x] every worklist processing branch proved to preserve the executable
    abstraction contract, ordered union-find forest, and live-component
    formula consistency;
  - [x] structural ownership proved to make every premise parent and every
    compound producer unique;
  - [x] exact frontier transport proved for successful par and tensor
    firings, including tensor representative changes and the newly created
    conclusion frontier;
  - [x] pending-premise coverage proved across every axiom start, every
    pop-and-process core branch, and the complete fuel-bounded production
    worklist run;
  - [x] canonical empty initialization proved to establish the bundled
    abstraction, ordered-parent, component/formula, and pending-frontier
    invariants used by the production worklist;
  - [x] atomic pop-and-process coverage proved for a submitted connective
    head: pop preserves every other classification, par/tensor firing
    transports it through exact consumer fan-out, tensor union preserves old
    shared-token classes, and processed-head reclassification restores full
    coverage;
  - [x] reverse consumer-table provenance proved for dependency fan-out;
    queue provenance and waiting-par provenance make every popped head a real
    connective; the bundled core, scheduler, flag, carrier, and provenance
    invariant is preserved through every finite production worklist run;
- [x] exact queue/waiting membership-to-flag completeness and duplicate-free
    registries preserved through enqueue, registration, requeue, pop,
    processing, and every finite run; both registry lengths are bounded by
    the submitted-link carrier;
- [x] exact transition and finite-run enqueue accounting, culminating in
    `linkAttempts + residual queue length = initial + dependency +
    waiting-requeue insertions` from canonical initialization;
- [x] cumulative enqueue-source bounds and worklist fuel sufficiency:
  - [x] every successful dependency fan-out inserts at most one distinct link;
  - [x] proof-only firing history is duplicate-free, submitted, marked, and
    exactly counted, hence successful firings are bounded by the link carrier;
  - [x] each full waiting requeue inserts at most the old registry length, and
    the production invariant bounds that registry by the link carrier;
  - [x] initial, dependency, and waiting sources fit `n(n+4)+1`;
  - [x] any residual queue implies exact fuel exhaustion, so source bounds plus
    insertion/pop conservation prove the canonical final queue is empty;
- [ ] correct-quiescent-state progress theorem;
  - [x] combine canonical queue exhaustion with scheduler coverage so every
    submitted but unfired connective yields an exact idle-premise,
    distinct-thread waiting-par, or same-thread tensor-deadlock witness;
  - [x] choose an unassigned conclusion of least formula complexity, recover
    its concrete submitted source link, and use strict premise complexity to
    rule out the idle-premise case;
  - [x] prove semantic-thread connectivity in the active all-left reference
    subgraph and use declarative reference-switching acyclicity to rule out
    same-thread tensor deadlock in a nonfinal reachable state;
  - [x] prove reachable markings causally closed, prove every active retained
    edge stays inside one token class, and derive exact equivalence between
    active-reference connectivity and union-find thread equality;
  - [x] strengthen the remaining waiting-par witness with a proof that its two
    marked premises have no active reference walk between them;
  - [x] prove occurrence-aware tree-edge exchange, flip exactly the submitted
    par occurrence, and obtain a reference simple path between its premises
    which avoids the par conclusion;
  - [x] turn active-component separation along that path into a concrete
    unmarked internal occurrence, distinct from both marked premises and the
    waiting par conclusion;
  - [x] expose an exact retained reference-edge occurrence directed from a
    marked path vertex into an unmarked target;
  - [x] select the first such frontier, retain its entirely active prefix,
    and prove that its source carries the waiting par's left-premise token;
  - [x] read the path backward to select the last inactive frontier, identify
    its marked target with the right-premise token, and derive one exact
    ordered decomposition with two distinct boundary occurrences;
  - [x] classify that boundary by an exact submitted-link lookup, excluding
    axiom and reverse-connective cases and retaining a forward par/tensor
    premise-to-conclusion occurrence;
  - [x] derive the classified frontier connective's local scheduler status:
    par omitted-premise/unassigned or registered/distinct-token, and tensor
    opposite-premise/unassigned after deadlock exclusion;
  - [x] derive that same local scheduler classification at the reverse-oriented
    right frontier;
  - [x] cut at the first unmarked-to-marked reentry, proving every intervening
    traversed occurrence has unmarked endpoints and retaining the exact local
    scheduler classifications at both boundary orientations;
  - [x] retain the globally minimum-complexity unassigned waiting conclusion
    and normalize both boundary classifications: each boundary target is
    strictly above that minimum or the boundary is an exact registered
    distinct-thread par;
  - [x] prove by strong induction that every unassigned occurrence reaches a
    concrete registered waiting par through strict formula-complexity
    descent, and retain such non-increasing chase endpoints for both
    orientations of the exact first inactive block;
  - [x] generalize the path/frontier/chase construction to every registered
    waiting par, prove that every dependency endpoint lies in the finite
    formula carrier, and prove that a dependency iteration of
    `formulas.size + 1` occurrences repeats a waiting conclusion while
    retaining every adjacent dependency witness;
  - [x] extract one concrete nonempty closed dependency segment with
    `earlier < later ≤ formulas.size`, equal endpoint conclusions, every
    vertex registered as waiting, and the full witness for every interval
    edge;
  - [x] retain every exact source-connective/premise step in the
    formula-descent chase inside each dependency, prove path composition and
    non-increasing endpoint complexity, and prove strict decrease for every
    nontrivial chase;
  - [x] lift every formula-premise descent step to its exact full
    occurrence-graph backward edge, compose nontrivial chases into
    vertex-simple internally cusp-free paths, retain unassigned evidence for
    every visited occurrence, retain the occurrence-exact lifted frontier,
    and exclude immediate frontier reversal by the first tail edge;
  - [x] classify the deterministic mask at the exact lifted full-edge index,
    transport the corresponding par/unique incidence colors, use structural
    typing and unique producer ownership to synchronize the first formula
    tail, propositionally bind the classified frontier and tail head to the
    actual last/first occurrences in the composed segment, and classify every
    nontrivial frontier/tail transition as a genuine par cusp or
    tensor-colored free turn;
  - [x] package every dependency as an exact composable complete-graph segment
    from its source waiting conclusion to its target, using the reversed
    source-par incidence, retained reference-prefix lift, and all-backward
    formula tail;
  - [x] concatenate the selected finite family into a genuinely nonempty
    closed occurrence-aware `fullGraph` walk with propositionally equal
    start/end conclusions;
  - [x] define exact-occurrence `Graph.EdgeWalk.NoImmediateReverse`, prove its
    duplicate-key, constant-orientation, and append rules, and prove every
    individual dependency segment internally nonbacktracking across all four
    internal regions/junctions;
  - [x] prove every immediate reversal at an adjacent dependency-segment
    junction forces the preceding dependency's formula chase to be reflexive;
  - [x] bind that reversal to the exact retained-frontier/full-edge occurrence
    and the next waiting par's source incidence, then cancel one such exact
    reverse pair while preserving endpoints, every remaining occurrence, and
    the nesting witness;
  - [x] iterate exact-occurrence cancellation over internal and cyclic closing
    junctions, preserve endpoints and surviving occurrences, and derive the
    exhaustive empty-or-cyclically-nonbacktracking closed normal form;
  - [x] retain a proof-relevant tree of every internal and rotated closing
    cancellation, and prove that an empty normal form contains the exact-index
    reverse of every represented directed-edge value in the original
    dependency obstruction (membership, not yet a positional bijection);
  - [x] propagate all-left reference retention for every forward occurrence
    through the concatenated dependency walk, and deduce that every original
    edge index is retained in the empty-normal-form branch;
  - [x] expose the deterministic reference switching's `IsTree` theorem and
    transport the empty branch's original nonempty closed exact-occurrence walk
    into that reference graph; retain the nested-backtracking possibility
    explicitly rather than treating a closed tree walk as an immediate
    contradiction;
  - [x] preserve exact compacted indices, stored edge values, orientations, and
    cyclic nonbacktracking through arbitrary occurrence masks; expose every
    valid switching of a correct certificate as a tree; prove a par-pair-sparse
    nonempty normal form impossible; and extract a concrete surviving
    left/right par pair whose omitted right occurrence is necessarily backward;
  - [x] prove cyclically nonbacktracking traversals are fixed points of the
    proof-relevant normalization; when a nonempty traversal normalizes to
    empty, extract an exact internal or closing cancellation site in the
    original representation; and prove that an internal cancellation in the
    append of two individually nonbacktracking pieces must cross their unique
    junction;
  - [x] preserve the full indexed dependency-segment decomposition through the
    empty branch, localize its exact cancellation site to an adjacent or cyclic
    segment junction, and prove that the junction is the preceding
    dependency's exact retained reflexive end and the following waiting par's
    stored left incidence;
  - [x] choose the first repeated waiting conclusion, preserve injectivity of
    all earlier chain positions, and prove that the empty-branch family is a
    simple dependency cycle with at least two distinct waiting-par nodes;
  - [x] retain the exact first marked-to-unmarked frontier and its assigned
    prefix in every dependency, prove that a frontier entering a registered
    waiting par has a reflexive formula chase, pair every source reverse with
    the unique cyclic predecessor's last occurrence, and conclude that every
    segment in the fully cancelling simple cycle ends at its reflexive
    frontier;
  - [x] preserve the occurrence-indexed full-segment decomposition through the
    finite family, remove each exact source/frontier pair to obtain the
    deterministic residual core, prove its endpoints share one live token,
    chain it to the cyclic successor, and prove every residual-core edge has
    both endpoints assigned before the first inactive frontier;
  - [x] prove each residual core is nonempty by combining exact par-target
    annotations, structural one-parent ownership, and injectivity of the
    at-least-two-node dependency cycle;
  - [x] inherit no-immediate-reverse from each containing dependency segment
    and record nonemptiness plus internal reduction in the deterministic
    active residual-core family;
  - [x] compose that deterministic family into a nonempty closed full-graph
    walk based at the first residual source premise;
  - [x] prove all core-only occurrences reference-kept, rule out a nonempty
    cyclically nonbacktracking normal form in the reference switching tree,
    and localize the forced empty-normal-form cancellation to a cyclic
    junction between two nonempty internally reduced cores;
  - [x] turn that cyclic list junction into an exact in-bounds dependency
    index, reconcile the cyclic successor's concrete segment/source witness,
    and expose the strict two-layer occurrence word
    `inner, outer, outer.reverse, inner.reverse` across the two consecutive
    dependency segments; exact segment nonbacktracking rules out a degenerate
    inner/outer pair;
  - [x] preserve both original full-segment witnesses at the indexed strict
    nesting, align each `core ++ frontier` with its exact retained reference
    prefix by compacted index, edge, orientation, and target, and transport the
    inner reversal to the actual head of the successor reference-prefix walk;
  - [x] replace every reflexive dependency by the complementary exact
    backward-right-par/reversed-strict-suffix path, prove that path
    vertex-simple and nonempty, and prove it avoids the target waiting par's
    exact retained left occurrence;
  - [x] compose the simultaneous flips around the simple dependency cycle and
    prove the flattened walk closed, nonempty, and cyclically
    nonbacktracking, with every forward occurrence retained by the all-left
    reference switching;
  - [x] prove every flipped segment internally cusp-free and use exact
    target-left avoidance plus reference-switching occurrence uniqueness to
    exclude cusps at every adjacent and cyclic closing segment junction;
  - [x] prove that a reference-omitted occurrence in a flipped segment is
    uniquely its head, exclude the source par's retained left incidence from
    that same vertex-simple segment, and localize the unavoidable concrete
    par-pair conflict to two distinct indexed flipped segments;
  - [x] use first-repeat prefix injectivity to prove that the conflict
    conclusion is a non-start vertex reached in the distinct holder segment's
    target list, with exact retained-left and omitted-right edge/mask facts;
  - [x] split the holder segment at the conflict conclusion into incoming and
    outgoing vertex-simple paths, prove the incoming side nonempty, assign the
    retained-left occurrence to its orientation-correct side, and prove the
    paths meet only at the conclusion;
  - [x] order the two indexed conflict segments by an exact
    before/middle/after decomposition and cut the cyclic family at the shared
    conclusion into two closed full-graph arcs; prove the first arc nonempty,
    place omitted-right in it, assign retained-left to one of the two, and
    cover the original flipped occurrences up to cyclic rotation;
  - [x] retain an exact rotation witness and transport internal cusp-freedom
    to the rotated concatenation and each closed arc separately;
  - [x] bind the retained-left orientation to the exact chord boundary:
    forward retained-left is the incoming path's last edge and forms a par
    cusp with the omitted-right first-arc head, while backward retained-left
    is the outgoing path's first edge and the nonempty second arc's head;
  - [x] classify the backward chord-closing turn: prove the second arc
    closing-cusp-free, cyclically nonbacktracking, nonempty, and strictly
    shorter than the original flipped walk;
  - [x] transport pointwise scheduler provenance and forward reference
    retention to the shorter second arc, re-extract its backward-right par
    obstruction, and locate that recursive chord at two distinct classified
    scheduler segments with the shared conclusion at one segment's start and
    internally in the other;
  - [x] package an arbitrary recursively cut closed subarc with internal and
    closing cusp-freedom, forward reference retention, pointwise indexed
    scheduler provenance, and its scheduler-located par obstruction; rotate
    omitted-right to the head and prove that a backward retained-left produces
    a strictly shorter state with every invariant preserved, while recording
    the exact rotation and contiguous smaller interval as a proof-relevant
    cyclic-interval descent step;
  - [x] close the backward recursion by traversal-length well-foundedness and
    prove that every initial fully reflexive flipped dependency family reaches
    a terminal forward retained-left par-cusp interval with a complete interval
    trace back to the original family, without ordinary loop erasure;
  - [x] strengthen that terminal object with the exact complementary cyclic
    interval; prove the complement nonempty, closed, internally cusp-free, and
    strictly shorter, and prove that every closing cusp on it is the exact
    last/first reversal rather than a second nontrivial par cusp;
  - [x] strip exact first/last reverse shells proof-relevantly while retaining
    the exact positional context and length equation; transport the normalized
    core's occurrence-indexed scheduler provenance, recurse through every
    cusp-free nonempty core by strict cyclic-interval descent, and reduce the
    nesting argument to an empty shell core or a scheduler-located nontrivial
    closing-par core;
  - [x] make the terminal obstruction position-aware in both coordinate
    systems: retain exact before/middle/after decompositions inside the current
    cyclic interval and inside the indexed scheduler-segment family, bind the
    terminal cusp head/last/conclusion to that same witness, and remove
    independently selectable duplicate witnesses from the terminal base;
  - [x] define coordinate-exact scheduler occurrences carrying segment step,
    in-segment offset, and edge value; prove the tagged family erases to the
    original flattened traversal, is duplicate-free, and supports exact
    segment/offset lookup; project each positioned par obstruction to its exact
    right-head and left-offset scheduler tags; prove tagged cyclic cuts and
    descent traces erase to their edge-level counterparts;
  - [x] refactor the cyclic scheduler state and full backward-chord recursion
    to carry tags from the initial family: invert every surviving tag to its
    exact source lookup, recompute the next obstruction on retained
    coordinates, and reach the terminal forward cusp with a complete tagged
    state-and-interval descent;
  - [x] define coordinate-exact reverse-shell normalization, prove that its
    two endpoint coordinate lists erase to reverse traversals, and turn it into
    an exact tagged cyclic descent;
  - [x] bind the terminal generator, arc, complement, derived strict cut,
    closed complement walk, source-fixed reverse-shell normalization, and
    nested trace in one indexed tagged witness, so every later argument refers
    to the same visits even when edge values repeat; remove the terminal path's
    first generic `CyclicIntervalCut` positional lift;
  - [ ] exclude the finite terminal bases by exact local geometry and the
    scheduler-order contradiction:
    - [x] in an empty core, pair every exact visit with a distinct
      reverse-valued visit from a different scheduler step;
    - [x] retain the complement's exact closed-walk/cusp-free witnesses,
      prove every empty-core edge reference-kept, transport the nonempty
      complement into the reference-switching tree, and exclude every
      flipped-segment head from that complement;
    - [x] select a concrete reverse pair, orient it by strict scheduler-step
      order, and retain the exact before/middle/after segment decomposition;
    - [x] invert every positive offset to the concrete omitted-head/retained-
      suffix decomposition and retain the suffix walk to its segment target;
    - [x] expose the coordinate-exact head-skipping reverse chord: the
      zero-offset heads of both ordered endpoint segments occur in the initial
      tagged scheduler family and are absent from the empty-core complement;
    - [x] classify both chord endpoints by their exact scheduler source,
      complete reference-kept suffix walk to the next scheduler conclusion,
      and the exact retained-left occurrence of that target waiting par avoided
      by the whole segment; connect this terminal outcome to the global fully
      reflexive dependency-cycle extraction;
    - [x] transport both selected suffixes to the deterministic reference
      switching with exact compacted indices and orientations, and prove that
      their selected occurrences remain an exact reverse pair;
    - [x] split the empty reverse shell into nonempty opening and closing
      coordinate halves, transport each half to a reference walk through one
      midpoint, and prove the complete retained closing traversal is exactly
      the reverse of the retained opening traversal;
    - [x] exclude the empty shell directly: the last opening occurrence is
      followed at the midpoint by its exact reverse, forcing a cusp contrary
      to the inherited cusp-free complement;
    - [x] retain the surviving closing-par core's exact first and last
      scheduler tags, invert them to their source segment/offset lookups, and
      prove the forward last incidence reference-kept;
    - [x] bind those exact first/last tags to the same positioned par link and
      the same reverse-shell normalization core, retain the closed core as
      `first :: middle ++ [last]`, prove the endpoint steps distinct and
      `chainAt (last.step + 1)` different from the shared conclusion, and
      exclude both same-segment and segment-boundary coordinate adjacency of
      the artificial closing seam;
    - [x] retain a state-and-interval ancestry through every backward search,
      terminal complement, reverse-shell normalization, and recursive nesting
      step, with a projection to the older `CyclicIntervalDescent`;
    - [x] replace every bare backward-search cut with an explicit semantic step
      sharing the exact positioned right/left tags, both cyclic
      decompositions, backward orientation, retained suffix, and strict cut;
    - [x] bind each nesting complement, its derived strict cut, closed walk,
      reverse-shell normalization, and recursive trace to the exact indexed
      terminal forward-cusp split that generated it;
    - [x] assemble the exact terminal base, data-indexed global ancestry,
      closing outcome, and normalized endpoint split into one package with
      common base/complement-base/complement/normalized-core indices;
    - [x] lift the hidden `SchedulerTaggedTerminalComplementStepAt` frame into a
      seam-specific indexed object, or destruct and thread the same existential
      wrapper exactly once, without reselecting its arc, rotations, link split,
      occurrences, scheduler coordinates, or segments;
    - [x] replay the artificial closing seam outward through the exact terminal,
      backward, reverse-shell, nesting, and global-ancestry frames as a boundary
      cursor, from a boundary-zero empty-gap seed to a nonempty accumulated
      candidate gap in the initial tagged family;
    - [x] prove pointwise gap containment and preservation of every incoming gap
      occurrence through the replay, and transport the same terminal omitted
      arc's zero-offset backward head into the initial tagged family with its
      exact right-par incidence, omitted reference entry, classified segment,
      waiting-dependency semantics, and source conclusion;
    - [x] enrich that same stored replay with an occurrence-position endpoint
      zipper whose `gap` is the complete complementary arc between the fixed
      first and last tagged endpoint occurrences, without identifying it with
      the weaker candidate cursor gap;
    - [x] derive nonemptiness of every scheduler segment from the indexed
      flipped traversals and prove the initial-family exact gap nonempty:
      otherwise empty exact gap would imply the scheduler-coordinate adjacency
      already excluded by the closing endpoint witness;
    - [x] retain the first exact empty-to-nonempty gap-opening frame with its
      original replay index, nonempty `rightContext ++ leftContext` insertion,
      and explicit gap-splice branch; split the replay at one shared Type-valued
      base zipper and classify the opening as terminal or ancestry, with the
      ancestry branch carrying a proof that every terminal gap stayed empty;
    - [x] construct the canonical endpoint replay of the fixed terminal step and
      prove that it always first-opens there: the reverse-shell frame opens
      first when `closing ++ opening` is nonempty, and otherwise the nonempty
      omitted arc opens in the second frame;
    - [x] retain in that construction the exact reverse-shell equation, the
      omitted-right zero-offset backward anchor, the forward retained-left last
      occurrence of the outer terminal arc, and the canonical base gap
      `closing ++ taggedArc ++ opening`;
    - [x] retain the erased outer `taggedArc` as a closed `EdgeWalk` at the
      complement base and a `CuspFreeTraversal`, together with its exact cyclic
      closing cusp from the outer last occurrence to the anchor and the
      non-reverse endpoint fact;
    - [x] prove endpoint-gap sublist and membership monotonicity and transport
      the complete outer `taggedArc` in its original linear order through
      global ancestry to the initial tagged-family gap, retaining its
      omitted-right head and outer retained-left last occurrence;
    - [x] prove the generic ordered-decomposition lemma for a retained sublist
      with specified `head?` and `getLast?`, and apply it to exhibit the initial
      exact gap as
      `g0 ++ anchor :: g1 ++ outerLast :: g2` and the initial family as
      `CyclicFourPointDisplayAt firstTag lastTag anchor outerLast`;
    - [x] specialize to the complete initial
      `tagSchedulerFamily flippedSegments`: use its exact-coordinate `Nodup` to
      prove `[firstTag, lastTag, anchor, outerLast].Nodup`, while preserving the
      boundary that erased edge values and vertices may still repeat;
    - [x] retain the same outer positioned terminal choice in the endpoint
      replay, lift both the inner normalized closing witness and that outer
      witness through exact ancestry membership into the complete initial
      family, and return both with the four-point display and four-tag `Nodup`
      fact from one specialized theorem;
    - [x] audit and reject a generic contiguous token-age/proper-nesting/LIFO
      invariant for the actual flat scheduler. The stable small accepted
      regression first merges ages 0 and 2, and ordinary laminarity permits the
      separated order
      `firstTag → lastTag → anchor → outerLast` as sibling intervals.
      `tagSchedulerFamily.step` is a dependency-segment index, not firing age;
    - [x] reject exact concrete-state confluence on a derivation-generated
      correct certificate and reject structural-only confluence on a
      structurally well-formed certificate;
    - [x] record an uncommitted exploratory local audit of the candidate
      marked-domain/occurrence-thread quotient on 80 derivation-generated
      correct certificates: 2,734 reachable quotient states, 7,148 enabled
      critical pairs, 0 disabled pairs, and 0 quotient non-diamonds. No
      committed reproducer exists, so this is neither a release/CI gate nor a
      theorem;
    - [ ] prove preservation of `ResidualParsingWitness` under an arbitrary
      successful firing, or a local-confluence theorem at that quotient, and
      use it to exclude the tagged nontrivial closing-par base and discharge
      the remaining path-exposed waiting-par obstruction;
- [ ] pure worklist completeness and equality with `check`;
- [ ] fallback removal from the exact worklist decision;
- [x] bounded/tagged `NEXTAXIOM` checkpoint:
  - [x] build a reusable source-incidence index and prove exact submitted-link
    origin for every stored entry;
  - [x] prove structural singleton lookup for every in-bounds occurrence.
    `SourceIndex.Sound` alone is only provenance: the index deliberately
    records both endpoints of a malformed self-axiom in one bucket, whose
    multiplicity structural well-formedness excludes;
  - [x] retain exact submitted axiom index/endpoints, final tags, and trace on
    success, with tag-array size preservation, old-true-tag monotonicity, trace
    `Nodup`, input-false/output-true trace and endpoint tagging,
    input-unmarked endpoint proofs, and `trace.length ≤ fuel`;
  - [x] define the touched carrier as trace plus endpoints and prove successive
    touched carriers disjoint when the second successful call uses exactly
    `first.tags`; reset or replaced tags are outside the theorem;
  - [x] prove dynamic start refines Figure 5 under `OrderedParents` and test
    zero fuel, out-of-bounds, tagged and marked starts, missing and ambiguous
    sources, threaded-result-tags repeat rejection, and dynamic allocation;
  - [x] prove every successful search has an exact oriented submitted
    source-left route to the endpoint actually reached; lock the stored-right
    orientation by regression;
  - [x] prove local totality under `SearchClearThrough` with fuel greater than
    the start complexity, plus the full-initial-carrier `complexity + 1`
    corollary and a depth-two exact rank/fuel boundary regression;
- [ ] later-state `NEXTAXIOM` selection and preservation of its freshness
  premise; carrier-size-wrapper totality is not yet established;
  - [ ] replace global low-rank freshness with a route-local invariant that
    survives previously tagged axiom endpoints;
  - [ ] prove a formula-complexity/carrier bound before exposing a
    `nextAxiom?` totality corollary;
  - [ ] weaken the local theorem from full `Abstractable` to its actual
    mark-slot-readiness dependency when API reuse warrants it;
- [ ] faithful `σ`/`R`/`W` state and token-age sequencing:
  - [x] prove the first independent `SigmaAgePartition`/waiting-state layer:
    strictly increasing `σ` boundaries partition raw discovery ages;
    out-of-bounds/undefined/initialized-empty remain distinct, and strict empty
    `init` establishes `OperationalWaitingDomain`;
  - [x] retain the printed fresh-cell `newEnqueue?` only as a source-audit
    helper, and prove that production `operationalNewEnqueue?` instead
    initializes the old active boundary, leaves the fresh top undefined, and
    preserves `WellShaped` plus `OperationalWaitingDomain`;
  - [x] implement initial and typed later `RealizesSigma` reservations with
    exact submitted/search orientations, unmarked endpoints, threaded tags,
    and preservation of `ReservationInvariant`;
  - [x] implement the invariant-bound local Figure-7 pop/mark/mate-search/new
    pipeline over the operational waiting interpretation;
  - [x] define the generic canonical consumer/conclusion views and executable
    local `concl`/`nop`/`wait` plus successful `forward` transitions, including
    exact success witnesses and reservation-invariant preservation;
  - [x] define independent Boolean-free direct common-prefix, `concl`, `nop`,
    and `wait` relations, and prove executable soundness, completeness under
    `StructurallyWellFormed` plus `ReservationInvariant`, and output
    uniqueness;
  - [x] implement the bounded production-core `queuePar?`/`queueTensor?`
    component mutations and delayed-stack
    `prependReadyTop?`/`mergeTopReadyWaiting?`, with typed success witnesses,
    raw-unmarked conclusions, explicit local invariant preservation, and the
    deterministic list refinement
    `conclusion :: (payload ++ previousReady ++ activeReady)`;
  - [x] define the first non-circular state-based `SchedulerInvariant`:
    explicit structural well-formedness, exact live-component/`sigma` domain,
    extensional ready/frontier correspondence, global live-frontier and
    queued-occurrence uniqueness, queued raw-unmarkedness, observable causal
    produced-conclusion/premise marking, delayed pending-premise coverage,
    exact delayed-par waiting spans (including raw-unmarked conclusions), and
    production-tree connective-counter accounting; prove empty and successful
    initialization for structurally well-formed certificates, including
    search/submitted orientation reversal;
  - [x] prove the synchronized common `PreparedStep` preserves every current
    state-only `SchedulerInvariant` field, using an exact active-top
    `sigmaBoundary?` lemma, extensional ready/frontier removal, global queue
    `Nodup`, waiting-span transport, and the selected live-component witness
    for newly marked pending-premise coverage; derive exact/executable
    `concl`/`nop` preservation through `prepared.after`;
  - [x] add proof-only occurrence-faithful internal component/link provenance:
    exact submitted link indices, exact first-occurrence focuses, complete
    owned vertices, local `Nodup`, cross-live-slot disjointness, exact
    representative-slot ownership for every marked owned vertex, same-frontier
    coverage for every unmarked owned vertex, and reverse ownership of every
    concrete raw mark; prove formula-consistency soundness, exact axiom
    reservation, local par/tensor queue extension, and repeated-label alias
    rejection;
  - [x] integrate `ComponentForestProvenance` into `SchedulerInvariant`;
    establish empty/initial forests and preserve the exact forest through the
    common prepared raw mark and therefore exact/executable `concl`/`nop`;
  - [x] preserve the complete current occurrence-exact state-only invariant
    through every successful deterministic `NewStep` and successful executable
    `new?`; discharge exact submitted-link/endpoint freshness for the appended
    axiom component and transport every current component, ready/queue, causal,
    waiting-span, pending-premise, and counter field;
  - [x] extend whole-forest and state-only preservation through every successful
    local `WaitStep` and executable `wait?`; prove the inserted conclusion fresh
    and raw-unmarked globally and add the exact positional par waiting span;
  - [x] implement successful typed `ForwardStep` and executable `forward?` with
    exact submitted-par identity and the non-strict paper guard
    `selectedRawAge ≤ mateRawAge`; preserve the complete occurrence forest,
    live frontier, ready/waiting queue, waiting-span, pending-premise, and
    fired-counter fields. Keep active-ready `Nodup` as a fail-closed shape check
    only, and lock the non-equality boundary plus typed
    `init → nop → forward → concl` regressions;
  - [ ] prove later-state `new?`/`wait?`/`forward?` applicability and totality;
    these successful-step preservation theorems are not dispatcher progress;
  - [x] define an independent Boolean-free `ForwardRule` and prove direct
    executable soundness, structurally valid completeness, iff, and output
    uniqueness with the fail-closed ready-list shape premise factored out;
    derive that premise and the higher-level iff from `SchedulerInvariant`;
  - [x] connect the tensor and stack primitives for the bounded empty-cell
    `UnifyEmpty` slice. Retain the exact submitted tensor position/orientation,
    prove the raw-age interval `j ≤ μ(mate) < i` yields representatives `j/i`,
    orient `parent[i] := j`, reject nonempty waiting cells, and prove direct
    soundness plus structurally valid completeness/iff/output uniqueness with
    the ready-list `Nodup` premise explicit. Preserve the complete
    `ReservationInvariant`, including exact `RealizesSigma` transport;
  - [x] preserve `ComponentForestProvenance` and every semantic
    `SchedulerInvariant` field through successful typed/executable bounded
    `UnifyEmpty`, including exact survivor/retired forest ownership and all
    live-frontier, queue/waiting/pending, and counter fields;
  - [x] construct/activate the exact par component for strict-singleton
    `W(j) = [c]`, retaining its exact submitted producer slot; prove atomic
    prepare/tensor/activation/drain semantics, direct/executable
    correspondence, output uniqueness, exact `+2` counter accounting, and
    full reservation/scheduler-invariant preservation. Reject empty and
    length-at-least-two payloads;
  - [x] add the local typed head-to-tail production-core fold that
    constructs/activates every listed par, has exact direct/executable
    correspondence and output uniqueness, preserves the stated core fields,
    and accounts for `payload.length` par constructors. This proves neither
    list-order independence nor scheduler applicability;
  - [x] integrate the local fold into atomic arbitrary `W(j)` `UnifyPayload`,
    with exact `1 + |W(j)|` project connective-counter accounting and
    `ReservationInvariant` preservation. Keep structural validity and the
    final-ready `Nodup` condition explicit for direct completeness/iff. The
    stored order fixes the executable derivation nesting but proves no
    order-independence, commutativity, or paper temporal order. The old
    empty/singleton bridges are one-way same-output compatibility only;
  - [x] preserve the new semantic scheduler fields through every successful
    arbitrary-payload atomic composition from a full input
    `SchedulerInvariant`, using the fixed-final-stack transient gap rather than
    asserting the invariant for physical tensor/fold intermediates;
  - [x] integrate canonical successful
    `concl`/`nop`/`new`/`wait`/`forward`/general `UnifyPayload` into one
    priority-aware dispatcher and proof-carrying certified history, excluding
    legacy empty/singleton unifiers as duplicate branch tags;
  - [x] augment that exact dispatcher history with branch-indexed tag evidence:
    five stable rules preserve the complete tag array, `new` retains its exact
    `NEXTAXIOM` touch and submitted axiom slot, and the global history proves
    true-tag iff recorded touch, touched-set separation, monotone growth,
    touched-history independence, and submitted-slot `Nodup`. Derive the
    augmentation from `ReachableByImplementedDispatcher`; do not read
    state-only tag-size alignment as provenance or claim the concrete all-true
    replacement has separately been proved unreachable;
  - [x] derive conditional input-only arbitrary-payload applicability from
    pure `UnifyPayloadEnabled` plus full `SchedulerInvariant`, including an
    invariant-preserving existence result and counterexamples to the invariant
    alone implying enabledness;
  - [x] derive conditional input-only applicability for stable `concl`, `nop`,
    `wait`, and `forward`, including full-invariant outputs and the scoped
    submitted-par `nop`/`wait`/`forward` trichotomy. Keep conclusions,
    tensors/`new`, unification, completed buckets, priority, and arbitrary work
    outside that local classification;
  - [x] define the marked-tensor predecessor invariant over every ready or
    waiting `FutureWorkAt`, prove empty/init and
    Prepared/Concl/Nop/canonical-New preservation, and specialize it to
    `ReadyHeadInput`. The projection eliminates the exact residual whenever the
    invariant is supplied; it does not establish full-history availability;
  - [x] extend that branch prefix through canonical Wait. Retained work
    transports across its prepared and destination updates, while private Wait
    geometry constructs the predecessor for the inserted conclusion;
  - [x] expose the conditional child-anchor bridge and extend the branch prefix
    through canonical Forward. The bridge leaves its separation and anchor
    premises explicit; private Forward geometry discharges them for an
    already-successful typed step under correctness, the scheduler invariant,
    canonical history, and the supplied prior predecessor invariant;
  - [x] extend the branch prefix through canonical UnifyPayload. A carrier-free
    raw touch theorem supplies created-conclusion separation; retained evidence
    transports across the final sigma pop, moved active work is contradictory,
    and created work uses final component provenance plus the conditional
    child-anchor bridge. The theorem consumes an already-successful typed
    branch, correctness, the complete scheduler invariant, canonical history,
    typed dispatch, and the prior predecessor invariant;
  - [x] package the closed successful-rule prefix as complete
    canonical-history preservation. The induction handles empty/init and all
    six successful dispatcher branches, the executed-history wrapper supplies
    the invariant under declarative correctness, and the reachable wrapper
    returns an exact successful dispatcher result at an explicitly supplied
    ready head;
  - [x] classify the remaining no-head shape as `ActiveTopDrained` and prove
    that every started correct dispatcher-reachable state yields exact dispatch
    or that residual;
  - [x] define `ActiveTopMarkedNonconclusionDebt`, establish it for empty/init
    and New, preserve it through Concl, and establish the non-global-created
    Forward/UnifyPayload branches under the prior complete scheduler invariant.
    New needs no additional scheduler-invariant premise. Prove that correctness,
    the complete scheduler invariant, `ActiveTopDrained`, and debt imply
    `core.allMarked = true`;
  - [x] characterize the Nop/Wait selected-away residuals and the global-created
    Forward/UnifyPayload exact-tail residuals as iff boundaries. The
    marked-nonconclusion presence antecedent only detects vacuity;
  - [x] define three-form branch-local continuation credit, prove fresh-event
    coverage, transport old credit through all six successful typed branches,
    and preserve its marked-nonconclusion predicate through every exact
    canonical tag history without a correctness premise. The eight public
    transports require structural well-formedness; Nop and New additionally use
    the old owner's concrete mark;
  - [x] finitely normalize continuation credit by strict formula-complexity
    ascent to a raw-mate, future-conclusion, or marked-global exit. Define a
    separate endpoint-bound raw/future-only locality receipt, prove that it is
    sufficient for active-top debt under structural well-formedness and queued
    vertices unmarked, and combine it with correctness, the full scheduler
    invariant, and `ActiveTopDrained` to derive `core.allMarked = true`. Do not
    claim the locality law is necessary or history-derived;
  - [x] prove that every successful typed Wait from a scheduler-invariant input
    refutes the unrestricted endpoint-locality law at its output. Therefore do
    not pursue that exact law as a full canonical-history invariant. Preserve
    the prior conditional implications, and infer neither post-Wait drainedness,
    reachable-Wait existence, nor failure of direct debt or Wait-compatible
    weakened laws. Keep the concrete `native_decide` trace research-only;
  - [x] normalize Nop and Wait debt to exact non-global `remainingTop`
    witnesses under prior debt and the complete scheduler invariant. Package
    the six exact current obligations in reset-aware `ActiveTopDebtTailLaw` and
    prove that the supplied law plus its canonical tag history yields endpoint
    debt. Do not infer the law itself from correctness, history, or reachability;
  - [x] reduce a correct selected `par` plus `SchedulerInvariant` to a
    non-global ready-tail witness or `ActiveCarrierParentEscape`, without
    assuming or deriving `ActiveTopDebtTailLaw`. Do not assert that the outcomes
    are exclusive; prove that no-tail forces escape; and use
    `CanonicalTagHistory` only to authenticate the escape's concrete mark. The
    public checkpoint contains no computational coexistence receipt;
  - [x] normalize the no-tail escape by source kind. For par, retain an
    authentic reservation anchor and a raw-sibling or strictly older parent
    continuation. For tensor, retain the escaped mark at the active boundary,
    its external sibling and parent, and the older marked-tensor predecessor
    invariant. Do not call
    this classification a tail witness or an impossibility theorem;
  - [x] consume canonical continuation credit to map both source branches to
    one temporal-outcome carrier with raw or strictly older endpoints;
  - [x] specialize actual no-tail Nop and Wait steps to an external-only
    temporal outcome by excluding the selected raw endpoint;
  - [x] attach the exact final retained commitment edge into the active top to
    both strictly older external branches, while preserving the raw branch;
  - [x] construct an exact active-carrier-to-external-endpoint path for both
    older branches and retain one concrete stored-edge boundary crossing;
  - [x] compose the full positive retained commitment interval and obtain an
    exact endpoint-to-active path plus outside-to-inside re-entry edge for
    ready future and older marked endpoints, preserving waiting and raw cases;
  - [x] classify each re-entry as the reverse of a submitted connective-parent
    edge and split its active-frontier target into selected raw, ready-tail raw,
    or concretely marked cases; explicit no-tail leaves selected or marked;
  - [x] authenticate every marked failure target in canonical raw-mark history
    at the active representative, and show that a re-entry path avoiding the
    current par conclusion cannot target the selected head;
  - [x] derive adjacent par-conclusion avoidance from an explicit child-event
    untouched callback, and classify the active equal-boundary edge as an
    inclusive avoiding-path or exact conclusion-to-selected/mate trace split;
  - [x] classify the complete positive retained interval as a composed avoiding
    endpoint path or one exact failed child edge with an authentic child-age
    selected/mate trace, located strictly before or at the final boundary;
  - [x] rule out strictly older selected and active-owned-mate traces, leaving
    only a stored-right trace to a mate outside the active owned carrier;
  - [x] specialize that older mate to exact external raw-unmarked work for Nop
    and exact external older-representative marked work for Wait;
  - [x] return those external endpoints through an exact active-carrier inbound
    parent edge and classify its target as selected raw, ready-tail raw, or
    concretely marked;
  - [x] under exact no-tail failure, eliminate the selected target from the
    strictly older stored-right Nop/Wait branch and retain a distinct
    authenticated marked target at the active representative;
  - [x] separate that marked target from the current mate and exclude the
    selected head as the mate of every exact connective view rooted there;
  - [x] bind the target's unique consumer to the inbound edge and normalize its
    continuation to raw-outside, older-future, or older-marked parent work;
  - [x] finitely normalize a marked non-global parent to raw-outside, an exact
    raw return to the current pair, older future work, or an older marked
    global conclusion;
  - [x] cyclically normalize the exact raw return to complete cancellation or
    an exact kept-prefix/omitted-continuation par pair;
  - [x] localize nonempty complete cancellation to the exact cyclic segment
    junction, refine it to the two oriented endpoint cases, pair every prefix
    occurrence with its exact reverse in the tail, and bind all reached prefix
    vertices and the omitted-right tail source to concrete marked
    nonconclusions;
  - [x] use retained-simple-path source uniqueness to order the continuation
    tail as the exact reverse traversal of the prefix;
  - [x] eliminate the exact raw return from the typed Nop target by combining
    concrete terminal marking for every nontrivial marked-conclusion chain with
    the Nop mate's raw-unmarked equation;
  - [x] refine the exact return retained by Wait to an authenticated first-step
    descent from the active carrier to a strictly older representative;
  - [x] order authentic prepared-selection raw marks and enrich the Wait first
    descent with earlier origin/mate events plus the sibling continuation exit;
  - [x] prove total comparison for distinct authentic raw marks and split the
    sibling continuation's marked-global endpoint before/after the outer-mate
    terminal while retaining raw and future exits;
  - [x] attach the exact cyclic-junction normal form to the full chain retained
    by the first causal descent, on the same switching path and target
    connective as the sibling causal classification;
  - [x] refine nonempty complete cancellation to both exact endpoint reverse
    junctions and classify its cyclic source at or before the authenticated
    outer terminal while retaining every residual;
  - [x] exclude the source-equals-base subcase of nonempty complete cancellation
    using retained-prefix edge-index uniqueness and reference-switching tree
    acyclicity, while preserving both endpoint junctions;
  - [x] re-root the first descent's sibling continuation after its shared
    marked non-global conclusion and use finite-chain terminal comparability to
    eliminate the marked-global-before branch while retaining raw, future, and
    marked-global-after exits;
  - [x] eliminate the marked-global-after sibling endpoint by combining the
    selected ready head's raw-unmarked lookup with canonical premise-before-
    conclusion raw-mark chronology; retain raw-mate and future-work exits;
  - [x] normalize the two open sibling exits under exact active occurrence and
    ready-tail failure: raw work is outside or exactly returns to the current
    selected/mate pair, while outside future work is strictly older;
  - [x] refine that older future endpoint to its exact scheduler location:
    either a ready bucket and live component frontier or a waiting span with
    its submitted par producer. Record the raw-unmarked conclusion and prove
    both consumer premises concretely marked without changing the raw-return
    cases;
  - [x] authenticate and strictly order both marked endpoint premises. Place
    the outside terminal at a strictly older representative and classify the
    mate as older outside or active-owned. For active-owned waiting work, force
    the terminal/mate to the older/younger span orientation and the younger
    boundary to the active boundary, while retaining ready work;
  - [x] eliminate the ready scheduler case for an active-owned mate by exact
    live-component ownership at the strictly older endpoint and active
    boundaries. Retain only its exact waiting return, while older-outside mates
    preserve both ready and waiting locations;
  - [x] rebuild that active-owned waiting mate as a marked, non-global
    active-frontier parent escape and normalize it under ready-tail failure to
    the existing parent temporal outcome. Thread the recursive status through
    the sibling target and typed Wait trace, without claiming endpoint
    elimination, a payer, or well-founded repeated normalization;
  - [x] eliminate the raw-continuation return from the exact active-owned
    waiting mate to its already marked older terminal in a two-constructor
    public result fixed to `consumer.conclusion` and containing only the
    strictly older future and marked cases. Return or store that narrowed
    carrier in the direct bridge and active scheduler status, with the
    `propext`-only `.activeCarrierOutcome` forgetful map to the broader parent
    temporal carrier. The direct bridge requires
    `CanonicalTagHistory`, `SchedulerInvariant`, active component lookup and
    occurrence, the outside-conclusion premise, and the waiting witness, but
    not `DeclarativelyCorrect`, `noTail`, or an explicit `mateActive` premise;
  - [x] under reference-switching connectedness and exact ready-tail failure,
    refine both external temporal endpoints with the exact
    `StrictOlderCommitmentSplit`, owned-to-external
    `ActiveCarrierExternalEndpointCrossing`, theorem-derived reverse
    `ActiveCarrierExternalEndpointReentry`, and selected-or-marked
    `ActiveCarrierExternalReentryFailureHistoricalStatus`. Keep crossing and
    re-entry existentially separate; do not identify arbitrary stored witnesses;
  - [x] under a structurally well-formed selected `par` whose side is
    `.storedRight`, eliminate the selected raw-unmarked alternative from the
    failure-conditioned historical re-entry status without an avoiding-path
    premise. Replace only that nested failure field with the authenticated
    marked target and carry it through the exact future/marked waiting
    endpoints, active future-work mate, continuation sibling target, and typed
    Wait older-mate trace. Preserve all unrelated trace branches and the
    causal-descent/cyclic-junction receipts;
  - [ ] eliminate the surviving distinct canonical-history-authenticated mark
    at the active representative or derive the exact distinct payer needed from
    it. The current checkpoint supplies no avoiding witness or aligned re-entry
    path. Then eliminate the remaining cyclic residuals, other finite exits,
    broader exact selected/mate raw returns, and equal-final trace branches, and
    close the history-tail law. This gate remains before marking incompleteness
    can imply unconditional full-rule progress, completion, termination,
    terminality, later-state totality, or Figure-7 pure-worklist completeness.
    Queued-head
    separation is already globally available for every structurally well-formed
    canonical history by empty/init and all six rule-preservation cases;
    mate-region and raw-mark invariant
    availability, branch applicability, recursive-fallback removal,
    sequentialization, faithful token-age scheduling, and whole-program
    linearity remain open.
    Given both strict invariants, adjacent callbacks
    and arbitrary positive intervals ending strictly before the candidate are
    now automatic. Every event whose current representative is strictly below
    the candidate is now split at the candidate's immediate predecessor.
    StoredRight closes the final edge. The BlockerAdvance theorem first exposes
    a complete avoiding path, a strict current-representative mate-touch
    advance, or the exact equal storedLeft callback failure; finite maximality
    then eliminates the advance. Resolving the remaining inclusive callback
    failure and closing the created-candidate raw seams remain open;
  - [x] prove exact source-left complexity descent and final-step
    decomposition, exclude a recursive visited-route return to the selected
    ready head under structural well-formedness, and remove that selected-head
    branch from the visited blocker classifier;
  - [x] under reference-switching acyclicity, embed the complete source-left
    route plus terminal axiom into an exact simple path, exclude the selected
    tensor conclusion by complexity, and rule out terminal-partner/head
    equality by an occurrence-aware tensor cycle; expose the
    `DeclarativelyCorrect` corollary and full-region blocker classifiers;
  - [x] recover every global canonical touch as one exact historical
    initialization/new search and expose its submitted slot, oriented route,
    and complete historical source-left region, without inferring current
    ownership or raw age;
  - [x] index those authentic reservation events chronologically by immutable
    raw age, prove the exact allocated range and lookup equations, reconcile
    chronological submitted slots with the reverse legacy history, and map
    every touch to an event that itself touched the vertex, without inferring
    final component realization or current ownership;
  - [x] characterize exact empty/init/operational-new histories and prove tag
    iff recorded touch, whole-history submitted-slot non-reuse, and
    reservation-count alignment; this characterizes neither the implemented
    local non-reserving rules nor the remaining unimplemented rules, and the
    preservation record alone still admits reset tags;
  - [ ] formulate and preserve guards over raw assigned ages, not
    representatives;
- [ ] full sequential scheduler correctness and completeness;
- [ ] full implemented-operation complexity theorem;
- [ ] adversarial, differential, downstream, API, and trust audits.

`CyclicIntervalDescent` alone is deliberately not used as a convexity
principle. A strict cyclic interval of a strict cyclic interval can wrap around
the first cut's artificial boundary; for example, an outer interval
`[b, c, d]` can be rotated and cut to `[d, b]`, which is not a cyclically
contiguous subinterval of the original linear representation when outside
elements separate `d` from `b`. Therefore a proof that a head-free terminal
state lies in one scheduler step would be false for the generic descent type.
The remaining proof must retain and use the specialized omitted-right chord,
terminal-complement, and reverse-shell endpoint witnesses that generated each
cut. Backward-search cuts satisfy this condition: their types bind the larger
state's positioned obstruction to the exact scheduler tags, rotation, retained
  suffix, and strict interval. The terminal step now satisfies it as well: one
  indexed witness fixes its generator, arc, complement, derived cut, closed walk,
  source-fixed reverse-shell normalization, and nesting trace. The terminal path
  therefore no longer uses the generic `CyclicIntervalCut` lift to select a tagged
  complement. A data-indexed global result now packages the terminal base,
  exact ancestry, and closing outcome under the same
  `(base, complementBase, taggedComplement, taggedNormalized)` tuple; the
  endpoint split is indexed by that same `taggedNormalized`. The structural
  replay consumes each stored `SchedulerTaggedTerminalComplementStep` wrapper
  exactly once, threads the same hidden
  `SchedulerTaggedTerminalComplementStepAt` witness, and folds the exact
  reverse-shell, backward-search, nesting, and global ancestry frames into a
  proof-relevant cursor and occurrence-position endpoint zipper. The cursor
  still carries only a candidate gap. The zipper instead records an exact cut
  of the current cyclic list and defines its gap as the complete complementary
  arc between the fixed tagged endpoints. These two gaps are not equated.

  Every indexed flipped traversal is nonempty. With that hypothesis, an empty
  endpoint-zipper gap in the initial tagged scheduler family implies exactly
  the same-segment or segment-boundary coordinate adjacency already excluded by
  the closing endpoint witness. The complete initial gap is therefore
  nonempty. The earlier generic replay theorem still records the first local
  empty-to-nonempty transition and its terminal/ancestry phase split, but the
  stronger canonical construction now fixes the terminal phase. Starting at
  the normalized core, its reverse-shell frame inserts
  `closing ++ opening`; if that list is nonempty it is the first opening.
  Otherwise the generator's nonempty `taggedArc` opens the gap in the second
  frame. The construction keeps
  `closing.map erase = reverseTraversal (opening.map erase)`, the omitted-right
  anchor at `taggedArc.head?`, the forward retained-left occurrence at
  `taggedArc.getLast?`, and the exact base gap
  `closing ++ taggedArc ++ opening`. Its erased `taggedArc` is a closed
  `EdgeWalk` at the complement base and satisfies `CuspFreeTraversal`
  internally, while the exact cyclic closing pair from the outer last
  occurrence to the anchor is a cusp and the two directed occurrences are not
  reverses. Thus the cusp is the wraparound closure, not an internal turn
  rejected by the linear cusp-free predicate.

  The shell-nonempty/empty distinction is made inside the construction of the
  first-opening proof. The returned
  `CyclicEndpointFirstGapOpeningInReplayAt` proposition does not yet provide a
  separately consumable sum identifying which frame opened or tying that frame
  to the omitted-right anchor origin. The anchor-origin theorem and the
  first-opening theorem are retained alongside one another, not connected by
  such a payload.

  Every endpoint splice preserves its incoming complete gap as a sublist.
  Consequently the complete outer `taggedArc` survives the ancestry replay as
  an ordered sublist of the initial scheduler-family gap, with its same
  omitted-right head and retained-left last occurrence. A generic theorem now
  combines exact `head?`/`getLast?` lookups with that `List.Sublist` evidence to
  decompose the initial gap exactly as
  `g0 ++ anchor :: g1 ++ outerLast :: g2`. Together with the endpoint zipper
  rotation, this yields
  `CyclicFourPointDisplayAt firstTag lastTag anchor outerLast`.
  `CyclicFourPointDisplayAt` is deliberately weak as a generic relation: all
  four intervening lists may be empty and the four displayed values may repeat
  unless separate hypotheses rule that out. Thus the result preserves the
  named occurrence order but proves neither contiguity, a fixed linear
  scheduler rank, crossing, nor a strict cyclic-betweenness relation. No
  model-specific scheduler-order/proper-nesting theorem yet turns the terminal geometry into the
  closing-par contradiction or correct-state progress.

  For the actual complete initial family, the separate hypothesis is now
  available: `tagSchedulerFamily_nodup` proves uniqueness of exact
  `SchedulerOccurrence` coordinates even when their erased `DirectedEdge`
  values repeat. Applying it to the four-point display gives
  `[firstTag, lastTag, anchor, outerLast].Nodup`. This is not an injectivity
  theorem for erasure and does not make the four erased edges, edge endpoints,
  or vertices distinct.

  The terminal endpoint replay also retains the exact outer positioned par
  choice which generated `anchor` and `outerLast`, rather than reconstructing a
  new choice from erased values. Exact membership through the ancestry replay
  lifts that same outer witness, and separately lifts the normalized inner
  closing witness, to the complete initial `tagSchedulerFamily`. The
  specialized closing theorem returns both positioned witnesses together with
  the four-point display and its four-tag `Nodup` fact.

  Those results expose the cyclic occurrence order
  `firstTag → lastTag → anchor → outerLast`. Pairing the inner
  `(firstTag, lastTag)` and outer `(anchor, outerLast)` endpoints makes the two
  pairs separated, not alternating; therefore this is not a crossing witness.
  `Nodup` does not make any of the four intervening intervals nonempty.
  Ordinary laminarity permits the separated pairs as siblings, and the stable
  three-axiom regression refutes contiguous token-age/LIFO behavior for the
  flat eager scheduler. Consequently no generic scheduler-order/proper-nesting
  contradiction remains as a valid flat-scheduler route. Exact-state and
  structural-only confluence are also refuted. A residual-witness theorem or
  confluence modulo the marked-domain/occurrence-thread quotient is still open
  for flat completeness; the uncommitted exploratory local 80-certificate
  audit is neither a reproducible release gate nor that theorem.
  The bounded/tagged `NEXTAXIOM` and dynamic-start checkpoint is kernel
  checked, including per-call trace/tag invariants, exact oriented routes,
  local rank-scoped totality, and touched-set disjointness for successive calls
  that strictly thread `first.tags`; reset tags are outside that theorem.
  The operational inactive-boundary `W` domain, initial/later reservation
  bridge, local pop/mark/new pipeline, and exact init/new execution history are
  now kernel checked. Exact local `concl`/`nop`/`wait`/`forward` and bounded
  `UnifyEmpty` and strict-singleton `UnifyOne` are also kernel
  checked, and every successful `wait`, `forward`, bounded `UnifyEmpty`, and
  strict-singleton `UnifyOne`
  preserves the complete occurrence-exact state-only invariant. Forward and
  bounded `UnifyEmpty` and strict-singleton `UnifyOne` additionally have
  independent Boolean-free direct relations and exact executable
  correspondence. The local arbitrary-payload production-core fold and atomic
  tensor/fold/drain `UnifyPayload` are also kernel checked; the latter preserves
  the complete occurrence-exact state-only invariant on every successful step
  from a full input invariant. Its separate input-only enabledness layer proves
  conditional scheduler-level applicability under `UnifyPayloadEnabled`, while
  making no physical-intermediate-invariance or invariant-alone-enabledness
  claim. The stable-rule input-only layer similarly proves conditional
  applicability and invariant preservation for `concl`, `nop`, `wait`, and
  `forward`; its exact-par trichotomy is local rather than a complete scheduler
  partition. A canonical priority dispatcher and
  certified proof-carrying history now record all six implemented successful
  rule families. Its canonical tag augmentation now proves exact touch
  provenance, global submitted-slot non-reuse, and exact reservation-event
  counting against final `nextAge` across that history. Later-state
  selection/applicability totality, whole-history route generalization, and
  unconditional full-rule reachability
  remain open for the separate linearity layer.
  Closing-par exclusion, progress, and pure-worklist completeness remain open.
  Planarity is not assumed.

The endpoint replay realizes the required zipper/gap invariant rather than an
adjacency claim. If a rotated parent view has the exact decomposition

```text
V = rotationSuffix ++ rotationPrefix
  = leftContext ++ inner ++ rightContext,
```

then the closing seam of `inner` expands in `V` through the exact outside gap

```text
last(inner) :: rightContext ++ leftContext ++ [head(inner)].
```

It is a parent-view closing seam plus a left generator leg when only
`leftContext` is nonempty, a right leg plus that seam when only
`rightContext` is nonempty, and two legs around one parent-seam obligation
when both are nonempty. Empty context on both sides is the identity case and
cannot represent a strict cut.  Re-basing from the rotated view also matters:
when both rotation pieces are nonempty, the view closing seam is the parent's
ordinary split adjacency; only an empty rotation piece carries the parent's
closing-seam obligation outward. The terminal complement is the one-sided
case, whereas every proper reverse shell is two-sided because its closing arm
is the reverse traversal of its nonempty opening arm. These decompositions are
stored in the exact replay and its first-opening witness. The canonical replay
now proves terminal-phase opening by the internal shell case split, retains the
exact reverse equation and closed cusp geometry, and transports the whole outer
terminal arc in original linear order through ancestry. Its returned
first-opening proposition does not expose the internal frame branch together
with the anchor origin. The resulting exact
`g0 ++ anchor :: g1 ++ outerLast :: g2` decomposition and generic
`CyclicFourPointDisplayAt firstTag lastTag anchor outerLast` still do not
establish contiguity, fixed linear rank, crossing, strict cyclic betweenness, a
scheduler-order/proper-nesting contradiction, or exclusion of the closing-par
base. In the complete initial family the four exact tags are now proved
`Nodup`, and both the inner and outer positioned witnesses are retained there;
the displayed order is nevertheless separated rather than crossing, and no
intervening interval is known nonempty. Ordinary laminarity therefore does not
exclude it, while the executable small regression rules out importing a flat
token-age/LIFO invariant. The remaining flat-completeness route is
residual-witness preservation or a theorem at the candidate quotient; the
Figure-7 stack route extends the existing bounded/tagged `NEXTAXIOM`
checkpoint with later-state selection, `σ`/`R`/`W`, and token-age sequencing.
Per-call trace/tag invariants, oriented route correctness, initial/local
rank-scoped totality, and strictly threaded touched-set disjointness are
already proved.
